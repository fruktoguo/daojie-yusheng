# Next Workspace

This directory exists only on the `next` branch.

Purpose:
- keep new `next`-only planning and implementation work in one place
- preserve the legacy production code in the same branch for reference
- avoid mixing future `next` scaffolding back into `main`

Suggested use:
- add migration notes here
- add branch-only scripts here
- add extracted `next` resources here before moving them into stable package locations
