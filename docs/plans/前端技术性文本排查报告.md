# 前端技术性文本排查报告

> 目标：排查 `packages/client` 中所有面向玩家可见的技术性文本、开发术语、内部实现暴露，收集问题并给出修改建议。
> 范围：普通玩家可见的所有 UI 文本（不含 GM 面板）。
> 原则：不直接修改，仅记录和建议。

---

## 一、高优先级（明显不适合玩家看到）

### T-001 FPS tooltip 暴露"客户端"和技术术语

- **文件**：`src/main-runtime-monitor-source.ts:311-312`
- **原文**：
  - `'客户端当前渲染帧率未采样'`
  - `` `客户端当前渲染帧率约 ${...} FPS，LOW ${...}，1% LOW ${...}` ``
- **问题**："客户端"、"渲染帧率"、"采样"、"LOW"、"1% LOW" 均为技术/性能分析术语
- **建议**：改为 `'当前画面流畅度未检测'` / `` `当前画面约 ${fps} 帧` ``，去掉 LOW 和 1% LOW 的技术指标，或改为"最低帧数"

### T-002 FPS tooltip 的 i18n 版本同样暴露技术术语

- **文件**：`src/constants/ui/i18n.generated.ts:879`
- **i18n key**：`shell.map-fps-rate.title`
- **原文**：`"客户端当前渲染帧率"`
- **建议**：改为 `"当前画面流畅度"`

### T-003 Ping tooltip 暴露"服务端主循环阻塞"

- **文件**：`src/constants/ui/i18n.generated.ts:894`
- **i18n key**：`shell.map-ping-rate.title`
- **原文**：`"通过当前访问域名检测游戏连接响应；当服务端主循环阻塞时，这里的数值也会被拉高"`
- **问题**："服务端主循环阻塞"是纯技术实现细节，玩家不应知道
- **建议**：改为 `"检测与游戏世界的连接延迟；数值越低越流畅"`

### T-004 Debug 面板标题为纯英文 "Debug"

- **文件**：`src/constants/ui/i18n.generated.ts:837`
- **i18n key**：`shell.debug-title-debug`
- **原文**：`"Debug"`
- **建议**：改为 `"紧急回城"` 或 `"脱困"`

### T-005 Debug 面板描述暴露 bug 场景

- **文件**：`src/constants/ui/i18n.generated.ts:838`
- **i18n key**：`shell.debug.reset-desc`
- **原文**：`"角色丢失或跑出视野时，可直接送回云来镇出生点。"`
- **问题**："角色丢失或跑出视野"是描述客户端 bug 的开发者语言
- **建议**：改为 `"若迷失方向无法行动，可直接返回云来镇。"`

### T-006 "出生点"为游戏开发术语

- **文件**：`src/constants/ui/i18n.generated.ts:836`
- **i18n key**：`shell.debug-reset-spawn`
- **原文**：`"回出生点"`
- **问题**："出生点"(spawn point) 是开发术语，与修仙世界观不搭
- **建议**：改为 `"回云来镇"` 或 `"返回初始之地"`

### T-007 性能设置说明堆砌大量技术术语

- **文件**：`src/constants/ui/i18n.generated.ts:1681`
- **i18n key**：`settings.performance.copy.overlay`
- **原文**：`"这里的配置只保存在当前设备。默认关闭；开启后会在地图顶部显示 FPS、LOW 与 1% LOW，方便排查全屏、缩放和特效变化带来的帧率波动。"`
- **问题**：FPS、LOW、1% LOW、排查、帧率波动均为技术术语
- **建议**：改为 `"此设置仅对当前设备生效。开启后会在地图顶部显示画面流畅度指标，帮助你了解不同场景下的画面表现。"`

### T-008 "帧率采样循环"暴露实现细节

- **文件**：`src/constants/ui/i18n.generated.ts:1682`
- **i18n key**：`settings.performance.desc.show-fps`
- **原文**：`"关闭时不显示浮层，也不会启动额外的帧率采样循环。"`
- **建议**：改为 `"关闭后不再显示流畅度指标。"`

### T-009 设置 tab 名 "UI" 为纯技术缩写

- **文件**：`src/constants/ui/i18n.generated.ts:1714`
- **i18n key**：`settings.tab.ui`
- **原文**：`"UI"`
- **建议**：改为 `"界面"`

### T-010 装备面板 trigger fallback 可能泄露英文标识符

- **文件**：`src/react-ui/panels/equipment/EquipmentPanel.tsx:98`
- **原文**：`triggerMap[effect.trigger] ?? effect.trigger`
- **问题**：当 trigger 不在映射表中时，会直接显示原始英文标识符如 `on_tick`、`on_cultivation_tick`
- **建议**：fallback 改为通用描述如 `"触发时"` 或 `"特殊条件"`

---

## 二、中优先级（偏技术但有一定理解门槛）

### T-011 设置 tab 名 "性能"

- **文件**：`src/constants/ui/i18n.generated.ts:1712`
- **i18n key**：`settings.tab.performance`
- **原文**：`"性能"`
- **建议**：改为 `"画面"` 或 `"流畅度"`

### T-012 "地图性能浮层"

- **文件**：`src/constants/ui/i18n.generated.ts:1686`
- **i18n key**：`settings.performance.section.overlay`
- **原文**：`"地图性能浮层"`
- **建议**：改为 `"画面流畅度显示"`

### T-013 "显示地图帧率浮层"

- **文件**：`src/constants/ui/i18n.generated.ts:1684`
- **i18n key**：`settings.performance.label.show-fps`
- **原文**：`"显示地图帧率浮层"`
- **建议**：改为 `"显示画面流畅度"`

### T-014 "渲染帧率上限已调至"

- **文件**：`src/constants/ui/i18n.generated.ts:1708`
- **i18n key**：`settings.status.target-fps-adjusted`
- **原文**：`"渲染帧率上限已调至 {fps}"`
- **建议**：改为 `"画面流畅度上限已调至 {fps}"`

### T-015 "UI 尺寸"和"高分屏"

- **文件**：`src/constants/ui/i18n.generated.ts:1721`
- **i18n key**：`settings.ui.desc.scale`
- **原文**：`"统一放大常用 UI 尺寸和字号，适合高分屏或 2K / 4K 屏幕。"`
- **建议**：改为 `"统一放大界面和字号，适合大尺寸或高清屏幕。"`

### T-016 属性面板"乘区参考底座撬动"

- **文件**：`src/ui/panels/attr-panel.ts:470`
- **原文**：`"基础值为 0 时，实际结果还会受到乘区参考底座撬动"`
- **问题**："乘区"、"底座"、"撬动"是数值策划内部术语，玩家完全无法理解
- **建议**：改为 `"基础值为 0 时，系统仍会给予少量基础加成"`

### T-017 属性面板"软衰减"和"移动预算"

- **文件**：`src/ui/panels/attr-panel.ts:473`
- **原文**：`"移动速度面板值已是软衰减后的有效移速，技能公式与移动预算都使用此值"`
- **问题**："面板值"、"软衰减"、"技能公式"、"移动预算"均为系统设计术语
- **建议**：改为 `"此为经过递减折算后的实际移动速度，所有移动和技能判定均以此为准"`

### T-018 移速 tooltip 中的"移动预算"

- **文件**：`src/ui/panels/attr-panel.ts:357`
- **原文**：`` `每息获得 ${movePoints} 点移动预算，约等于 ...` ``
- **问题**："移动预算"是内部移动力系统的抽象概念
- **建议**：改为 `"每息可移动 ${movePoints} 步，约等于 ..."` 或 `"每息移动力 ${movePoints}，约等于 ..."`

### T-019 i18n 中移速描述暴露"软衰减"和"移动预算"

- **文件**：`src/constants/ui/i18n.generated.ts:2050`
- **i18n key**：`attr.numeric.move-speed.desc`
- **原文**：`"决定每息获得的移动预算。500 以内按原始收益计算，超过后进入软衰减；大路、小路、草地、泥地与沼泽会按不同消耗结算。"`
- **建议**：改为 `"决定每息的移动能力。500 以内线性增长，超过后增幅递减；不同地形消耗不同步数。"`

### T-020 "六维境界乘区"

- **文件**：`src/ui/panels/attr-panel.ts:1634`
- **原文**：`` `六维境界乘区：${formatDisplayNumber(100 + numericValue)}%` ``
- **问题**："乘区"是伤害公式分区术语（加区/乘区），不是玩家应该看到的词
- **建议**：改为 `"六维境界加成：${...}%"`

### T-021 i18n 中"独立伤害乘区"

- **文件**：`src/constants/ui/i18n.generated.ts:2079`
- **i18n key**：`attr.special.combat-exp.desc`
- **原文**（节选）：`"...以及普通攻击 50% 到 200% 的独立伤害乘区。"`
- **建议**：改为 `"...以及普通攻击 50% 到 200% 的独立伤害加成。"`

### T-022 i18n 中"六维境界乘区"

- **文件**：`src/constants/ui/i18n.generated.ts:2087`
- **i18n key**：`attr.special.root-foundation.desc`
- **原文**：`"每点根基提供 1% 六维境界乘区，..."`
- **建议**：改为 `"每点根基提供 1% 六维境界加成，..."`

### T-023 战斗日志"原始伤害"暴露计算管线

- **文件**：`src/ui/chat.ts:301-302, 326-327, 492, 515`
- **原文**：
  - `"实际伤害 ${actualAmount}"`
  - `"原始伤害 ${rawAmount}"`
  - `"实际治疗 ${actualAmount}"`
  - `"原始治疗 ${rawAmount}"`
- **问题**："原始伤害"(raw damage) 是伤害公式计算的中间值，暴露了伤害计算管线
- **建议**：改为 `"造成伤害"` / `"减伤前伤害"` 或 `"基础伤害"` / `"最终伤害"`

### T-024 炼丹面板"基时"为设计术语

- **文件**：`src/ui/craft-workbench-modal.ts:3543`
- **原文**：`` `一炉 ${count} ${unit} · 基时 ${recipe.baseBrewTicks} 息 · ${presetText}` ``
- **问题**："基时"(base brew time) 是内部设计术语，玩家不理解"基时"与实际耗时的区别
- **建议**：改为 `"耗时"` 或 `"炼制时间"`

### T-025 强化面板"本地"暴露存储实现

- **文件**：`src/ui/craft-workbench-modal.ts` / `src/ui/craft-enhancement-view.ts`（多处）
- **原文**：
  - `"本地累计 ${records.length} 件 · 历史最高 +${highestLevel}"`
  - `"当前未选中强化目标。你仍然可以查看本地历史记录，累计强化 ${totalAttempts} 次。"`
  - `"当前还没有本地强化记录。"`
- **问题**："本地"指浏览器 localStorage，暴露了客户端存储概念
- **建议**：去掉"本地"二字，直接说"累计"、"强化记录"即可

### T-026 强化面板"总加速"

- **文件**：`src/ui/craft-enhancement-view.ts:453, 884`
- **原文**：`` `角色强化等级 Lv.${level} · 总加速 ${percent}` ``
- **问题**："总加速"是内部速率乘数的直接暴露
- **建议**：改为 `"强化速度加成"` 或 `"效率提升"`

---

## 三、低优先级（边界情况或轻微不适）

### T-027 邮件面板"气机已断"系列暴露重连机制

- **文件**：`src/ui/mail-panel.ts:617, 622, 629, 642`
- **原文**：
  - `'气机已断，正在重续...'`
  - `'续气未果，请重入天地再试。'`
  - `'气机已续，静待身显...'`
  - `'气机已续，重试呈报...'`
- **问题**：虽然用了世界观语言包装，但分步骤暴露了 session 恢复与操作重放的技术流程
- **建议**：简化为两个状态即可：`"连接中断，正在恢复..."` / `"恢复失败，请刷新重试。"` 或保持世界观风格但合并为 `"气机波动，稍候..."` / `"气机断绝，请重入天地。"`

### T-028 "React UI 脚手架" 面板标题

- **文件**：`src/constants/ui/i18n.generated.ts:3246`
- **i18n key**：`react.prototype.scaffold.title`
- **原文**：`"React UI 脚手架"`
- **问题**：纯开发术语，如果玩家能看到此面板则完全不理解
- **建议**：如果是开发专用面板，应在生产环境隐藏；如果需要保留，改为 `"新界面预览"`

### T-029 面板注册名 "调试"

- **文件**：`src/constants/ui/i18n.generated.ts:1442`
- **i18n key**：`panel.registry.debug`
- **原文**：`"调试"`
- **建议**：改为 `"脱困"` 或在生产环境隐藏此面板入口

### T-030 console.warn 中暴露模块名和技术描述

- **文件**：`src/map-memory.ts:190, 460`
- **原文**：
  - `'[map-memory] 本地存储不可用，已退回仅内存模式。'`
  - `` `[map-memory] 部分地图记忆未能恢复（已加载 ${loadedMapCount}/${storedMapCount}），已保留原始数据且停止本次会话持久化。` ``
- **问题**：虽然只在浏览器控制台可见，但暴露了模块名、持久化策略、会话概念
- **建议**：改为英文 debug 日志（如 `[map-memory] localStorage unavailable, falling back to memory-only`），或移除中文描述

### T-031 "地图画面帧率上限"

- **文件**：`src/constants/ui/i18n.generated.ts:1685`
- **i18n key**：`settings.performance.label.target-fps`
- **原文**：`"地图画面帧率上限"`
- **建议**：改为 `"画面流畅度上限"` 或 `"最高帧数"`

### T-032 "性能已复归原本"

- **文件**：`src/constants/ui/i18n.generated.ts:1706`
- **i18n key**：`settings.status.performance-reset`
- **原文**：`"性能已复归原本"`
- **建议**：改为 `"画面设置已恢复默认"`

### T-033 离线收益"本地保存失败"

- **文件**：`src/constants/ui/i18n.generated.ts:1198-1201`
- **i18n key**：
  - `offline-gain.toast.local-save-failed`：`"离线挂机收益已接收，本地保存失败，云端会保留并下次重发"`
  - `offline-gain.modal.hint.save-failed`：`"本地保存失败，云端仍会保留"`
  - `offline-gain.modal.hint.saved`：`"已存入本地浏览器"`
- **问题**："本地保存"、"云端"、"本地浏览器"暴露了存储架构
- **建议**：
  - `"离线收益已接收，记录保存失败，下次登录时会重新发放"`
  - `"记录保存失败，不影响收益"`
  - `"已保存"` 或直接去掉此提示

### T-034 小地图记忆中的"本地"

- **文件**：`src/constants/ui/i18n.generated.ts:2214, 2239-2245`
- **原文**（多条）：
  - `"仅保留本地探索记忆，未获得完整地图。"`
  - `"当前没有可删除的本地记忆"`
  - `"删除所有地图的本地记忆"`
  - `"删除全部本地记忆"`
- **问题**："本地"暗示了客户端存储实现
- **建议**：统一改为"探索记忆"即可，去掉"本地"前缀。如 `"仅保留探索记忆，未获得完整地图。"`

### T-035 设置保存提示"本机"

- **文件**：`src/constants/ui/i18n.generated.ts:1728`
- **i18n key**：`settings.ui.status.saved-local`
- **原文**：`"当前配置已自动保存到本机"`
- **建议**：改为 `"设置已保存"` 或 `"已自动保存"`

---

## 四、汇总统计

| 优先级 | 数量 | 核心问题 |
|--------|------|----------|
| 高 | 10 处 | FPS/Ping tooltip 暴露技术术语、Debug 面板开发用语、性能设置技术堆砌、装备 trigger fallback |
| 中 | 16 处 | 属性面板数值策划术语（乘区/底座/软衰减/移动预算）、战斗日志暴露计算管线、炼丹"基时"、强化"本地"/"总加速" |
| 低 | 9 处 | 邮件重连分步暴露、React 脚手架标题、console 中文日志、"本地"存储概念泄露 |

---

## 五、修改原则建议

1. **去技术化**：将所有"客户端"、"服务端"、"帧率"、"采样"、"浮层"等术语替换为玩家能理解的日常用语
2. **去实现化**：将"本地"、"会话"、"同步"、"持久化"等暴露存储/网络实现的词汇移除或替换
3. **去策划化**：将"乘区"、"底座"、"软衰减"、"移动预算"、"基时"等数值策划内部术语改为通俗表述
4. **世界观一致**：将"出生点"、"Debug"等与修仙世界观不搭的词汇替换为符合游戏设定的表述
5. **安全 fallback**：所有 fallback 文本不应暴露内部标识符（英文 trigger 名、ID 等），应使用通用描述兜底
