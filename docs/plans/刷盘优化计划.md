# 刷盘优化计划

> 目标：降低玩家刷盘与 flush ledger 的体积膨胀，明确各分域是否全量落盘，逐步把最重的域拆成更薄的写入路径。

## 完成记录

- 2026-07-11：将 runtime dirty → ledger 暂存 → 真源落库拆成明确的两阶段。staged 只表示 payload 已可靠进入 ledger，不能当作 persisted；同一 domain 仅在修订变化时再次暂存，ledger 采用 latest-wins generation CAS，完成、重试和续租均必须携带唯一 claim owner，避免已完成行被每 1.5 秒重复 UPDATE/TOAST 重写。
- 2026-07-11：玩家投影与 presence 统一使用进程内单调版本；同 owner/session 的旧 presence、旧直写投影在写 SQL 前先比较 recovery watermark，不再出现“数据行已经回退、watermark 却仍显示新版本”。正常 inline/worker 模式取消 runtime 的重复直写，由 durable flush task 统一消费。
- 2026-07-11：启动恢复先 drain presence，再 replay player/instance durable payload；历史 ownerless payload 不再信任 ledger 残留 owner。实例 lease epoch 变更、GM 迁移、catalog 接管均先冻结旧写并 replay 旧 epoch，再以 source token + epoch CAS 切换。
- 2026-07-11：关机在关闭连接池前停止接单、执行最终 staging、durable replay 与 pending=0 复核；任一失败保留实例 lease。通天塔连接池关闭不再伪装成成功，避免出现大量“跳过”日志后仍报告 drain 完成。
- 2026-07-11：outbox 消费加入 claim heartbeat/owner CAS；死信与 dedupe 保持事务原子。资产审计热表保留 30 天，archive 按事件创建时间分批保留普通资产 365 天、combat 90 天，避免 archive 无限进入逻辑备份。
- 已将玩家 projectable flush task 的 ledger payload 从“同一玩家多 domain 复用同一份组合 snapshot”改为“每个 domain 只保存该 domain-gated snapshot”。消费侧逐 task 按单 domain 写入 `PlayerDomainPersistenceService.savePlayerSnapshotProjectionDomains()`，避免一个重 payload 被同一玩家多个 ledger row 重复保存。
- 新增 `player-flush-payload-report`，with-db 下按 `player_flush_ledger` / `instance_flush_ledger` domain 统计 `payload_jsonb` 平均、最大、p95、payload row 数，并统计玩家分域 JSONB 细节列体积分布与 ledger relation size。
- 新增 `flush-ledger-retention-smoke`，with-db 下 seed completed ledger rows，验证旧 `payload_jsonb` 被置空、超过 row retention 的 completed 空 payload row 被删除。
- 本次 with-db 报告显示玩家 ledger 大头为 `inventory`（p95 11542B，max 11542B）、`attr`（p95 5498B，max 11911B）、`body_training/profession/progression`（p95 4-5KB 量级）；玩家分域真源 JSONB 列整体明显小于 ledger 中转 payload，例如 `player_inventory_item.raw_payload` p95 5B、max 65B。
- completed payload 清理会减少后续逻辑备份内容并释放 PostgreSQL 可复用空间；只有 `TRUNCATE` 或受控 rewrite（如维护窗口内的 `VACUUM FULL` / `pg_repack`）才保证 relation 文件物理缩小。
- 已将 `inventory` 真源写入从“全行无差别 upsert + 整玩家 stale cleanup”收敛为行级增量 apply：同 `item_instance_id` 数量变化只更新 `count/raw_payload`，格子变化通过临时槽 staging 后只移动 `slot_index`，未变行不刷新 `updated_at`。

## 目标

- [x] 找出玩家刷盘里最肥的分域和 payload 来源
- [x] 区分 ledger 中间态和最终真源写入
- [x] 明确哪些玩家分域是整组重写，哪些是增量 upsert + prune
- [x] 保持空覆盖、item_instance_id 修复、恢复语义不退化
- [x] 保持 flush ledger 的回收自动化，不误删 pending/retry/claim

## 现状分区

- [x] `player_flush_ledger` / `instance_flush_ledger` 只作为刷盘中间账本
  - 证据：flush task 消费侧读取 `payload_jsonb` 后写入分域真源表，并通过 `markFlushTaskFlushed()` 推进/清理账本状态。
- [x] `flush-ledger-retention` 负责自动回收完成账本
  - 证据：`FlushLedgerService.retainCompletedFlushLedger()` 已覆盖玩家/实例 ledger payload 清理和 completed row 删除，`flush-ledger-retention-smoke` 已通过。
- [x] `payload_jsonb` 进入 TOAST 是 ledger 体积膨胀主因
  - 证据：report 可同时观测 payload/TOAST 与 relation size；completed payload 清理本身保证逻辑数据减少和页可复用，relation size 的即时下降仅在伴随 truncate/rewrite 时成立。
- [x] `inventory` 是最重的玩家分域之一
  - 证据：with-db report 中 `inventory` 是玩家 ledger p95 最高 domain（p95 11542B）。

## 玩家分域写入类型

### 整组重写 / 单域整组 replace

- [x] `equipment`
- [x] `body_training`
- [x] `attr`
- [x] `combat_pref`
- [x] `active_job`

### 增量 upsert + prune stale

- [x] `inventory`（snapshot 输入，DB apply 已按 `item_instance_id` 行级 diff 更新 / move / insert / delete）
- [x] `wallet`
- [x] `map_unlock`
- [x] `market_storage`
- [x] `technique`
- [x] `buff`
- [x] `quest`
- [x] `auto_battle_skill`
- [x] `auto_use_item_rule`
- [x] `profession`
- [x] `alchemy_preset`
- [x] `enhancement_record`
- [x] `logbook`

### 轻量单行替换

- [x] `world_anchor`
- [x] `position_checkpoint`
- [x] `vitals`
- [x] `progression`

> 注：上述分类以当前代码事实为准。`inventory` 仍以 domain snapshot 作为输入，但 DB 写入已按行级 diff apply；`equipment/buff/quest/...` 的清理路径使用 snapshot key prune，不再发送裸整玩家 DELETE；空数组覆盖由显式 option 或 guard 控制。

## 开发任务

- [x] 统计各玩家分域的实际 payload 大小分布
- [x] 统计 `player_flush_ledger` 中各 domain 的平均 / 最大 `payload_jsonb` 大小
- [x] 识别 `inventory`、`buff`、`quest`、`progression` 的主要膨胀来源
  - `inventory`：ledger domain snapshot 中的 inventory item 集合，是当前最大玩家 domain payload。
  - `buff` / `quest`：ledger 中为 domain snapshot，最终真源为行级表 + `raw_payload` 细节列，report 可继续按列追踪。
  - `progression`：ledger payload 来自 progression bundle；最终真源热字段已列式化，低频细节在 `attr` / job / record 相关表承载。
- [x] 评估并落地 `inventory` 从整组重写继续拆成更细粒度写计划
  - 结论：已先落地 DB apply 侧 item-level diff；ledger payload 仍是 inventory domain snapshot，后续若继续变薄，可再把 ledger 输入拆成 item-level upsert/delete diff。
  - 最小实现：运行时背包先规范化成稳定行，再按 `item_instance_id` 和库内当前行做 key 级 diff；新增行 insert，只改数量/实例 payload 的行 update，格子变化行通过临时槽 staging 后 update `slot_index`，缺失行按 stale `item_instance_id` delete。
  - 语义边界：仍保留 `item_instance_id` 修复、锁定行负 `slot_index` 自避让、空覆盖拒绝与显式 overwrite guard；不对整包 JSON 做深度比对，只比较行级稳定字段签名。
- [x] 评估 `buff`、`quest`、`progression` 是否能继续拆薄
  - 结论：三者已受 domain-gated snapshot 与分域真源约束；短期优先继续观测 p95/max，若持续成为大头，再分别拆为 buff/quest row diff 或 progression 子字段 payload。
- [x] 保留 `replacePlayerInventoryItems()` 的空覆盖和修复语义
- [x] 保留 `replacePlayerEquipmentSlots()` 的空覆盖和修复语义
- [x] 保留现有 `prunePlayerRowsBySnapshotKeys()` 的回收语义
- [x] 保留 `flush-ledger-retention` 的自动回收语义

## 验收

- [x] 能明确说出哪些分域是全量、哪些不是
- [x] 能明确说出哪些分域是账本大头
- [x] `inventory` 的 DB 真源写入已具备行级增量 apply，ledger 输入继续保留下一步拆薄方案
- [x] flush ledger 回收后能减少逻辑备份内容并释放可复用空间；物理缩容遵循单独维护窗口
- [x] 不引入资产丢失、重复写入、空覆盖回退问题

## 风险

- [x] 过度拆分会增加写路径复杂度
  - inventory 已先落地 DB apply 侧行级 diff，并通过 smoke 约束空覆盖、未变行不刷新与 flush 消费路径；ledger 输入是否继续拆成 item-level diff 仍需单独评估。
- [x] inventory 的 item_instance_id 修复必须保留
  - 已由 `player-domain-persistence-smoke` / `flush-presence-payload-smoke` 覆盖关键路径。
- [x] 不能把 dirty / retry / claim 中的账本误删
  - retention 仅作用于 completed、未 claim、未 retry-delay 的 rows；smoke 已验证 completed payload/row 清理路径。
- [x] 不能把 ledger 当成最终真源
  - 已通过 report 和代码路径区分 ledger 中转 payload 与玩家分域表真源。

## 验证记录

- `pnpm --filter @mud/server compile`：通过。
- `pnpm --filter @mud/server smoke:player-inventory-incremental`：通过，覆盖同一玩家 inventory 行级 diff：数量/格子变化只触达变更行，未变行 `updated_at` 不刷新。
- `pnpm --filter @mud/server smoke:player-domain-persistence`：通过，覆盖分域写入、inventory stale-id delete、空覆盖显式清理和直接写入路径。
- `pnpm --filter @mud/server smoke:flush-player-payload`：通过，覆盖玩家 presence payload 与多 projectable domain 独立 snapshot payload 消费。
- `pnpm --filter @mud/server smoke:player-flush-payload-report`：通过，输出 with-db ledger/domain JSONB 体积分布与 relation size。
- `pnpm --filter @mud/server smoke:player-domain-empty-overwrite-guard`：通过，覆盖 inventory/wallet/equipment/market_storage/technique/buff/quest 空覆盖拒绝与 rollback。
- `pnpm --filter @mud/server compile && node packages/server/dist/tools/flush-ledger-retention-smoke.js`：通过，覆盖 completed ledger payload 清理与 completed 空 payload row 删除。
