# 性能优化清单

## 待处理
- [ ] `packages/server/src/runtime/world/combat/world-runtime-auto-combat.service.ts`
  - 问题：`resolveAutoBattleSelfSkillChoice()`、`resolveAutoBattleSkillChoice()`、`resolveAutoBattleDesiredRange()` 在遍历每个技能动作时，都会重复调用 `findPlayerSkill()`，而 `findPlayerSkill()` 会再次线性扫描 `player.techniques.techniques`。
  - 影响：同一轮自动战斗技能筛选中，会出现“动作列表遍历 × 技能列表遍历”的重复成本；当可用动作和已学技能都偏多时，这部分会放大热路径开销。
  - 处理建议：在进入筛选前为当前玩家构建一次技能索引，并在本轮筛选中复用；保持首个匹配技能的语义不变。
  - 判断：可在不改变设计和表现的前提下修复。

## 待决定

## 已完成
- [x] `packages/server/src/network/world-sync-map-snapshot.service.ts`
  - 已完成：`buildVisibleTileKeySet()` 去掉了对同一坐标的冗余 `getInstanceTileState()` 查询。
  - 结果：减少视野同步热路径中的重复查询和对象访问开销，不改变行为。
- [x] `packages/server/src/runtime/world/combat/world-runtime-auto-combat.service.ts`
  - 已完成：`selectAutoCombatTarget()` 预计算一次怪物敌对关系，并在同轮筛选中复用。
  - 已完成：候选 `metrics` 从两次 `reduce()` 改为遍历时单次累积。
  - 结果：减少自动战斗目标选择路径中的重复对象构造和重复遍历开销，不改变行为。
