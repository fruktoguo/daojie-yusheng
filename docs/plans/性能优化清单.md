# 性能优化清单

## 待处理

## 待决定





## 已完成
- [x] `packages/server/src/network/world-sync-map-snapshot.service.ts`
  - 已完成：`buildVisibleTileKeySet()` 去掉了对同一坐标的冗余 `getInstanceTileState()` 查询。
  - 结果：减少视野同步热路径中的重复查询和对象访问开销，不改变行为。
- [x] `packages/server/src/network/world-sync-threat.service.ts`
  - 已完成：`diffThreatArrows()` 改为按 key 一次性建索引后做差集。
  - 已完成：`buildThreatArrows()` 只在确有多项时排序。
  - 结果：减少威胁箭头同步 diff 的重复 key 生成和无意义排序，不改变行为。
- [x] `packages/server/src/network/world-sync-minimap.service.ts`
  - 已完成：`buildLegacyTileRows()` 改成按行收集字符后一次性 `join`。
  - 已完成：`buildMinimapMarkers()` 只在 markers 有多项时排序。
  - 结果：减少 minimap 静态快照的大图字符串分配和无意义排序，不改变行为。
- [x] `packages/server/src/concurrency/workers/encoding.worker.ts`
  - 已完成：`handlePathfind()` 只按 `(mapId, mapRevision)` 判定是否需要重建缓存，不再因 payload 里的 `walkable/traversalCost` 每次强制刷新 `cachedGrid`。
  - 结果：同一地图与 revision 下的重复寻路可以复用 worker 侧静态网格缓存，不改变行为。
- [x] `packages/server/src/concurrency/encoding-worker-pool.service.ts`
  - 已完成：维护 `activeWorkerCount`，用常量计数代替热路径上的重复数组扫描。
  - 结果：减少 worker 池提交和生命周期管理中的同步开销，不改变行为。
- [x] `packages/server/src/concurrency/instance-worker-pool.service.ts`、`packages/server/src/concurrency/persistence-worker-pool.service.ts`
  - 已完成：维护 `activeWorkerCount`，在启动、死亡、关闭时增减，避免每次提交和状态检查都重复扫描 worker 数组。
  - 结果：减少 worker 池高频调度入口上的同步开销，不改变行为。
- [x] `packages/server/src/runtime/world/combat/world-runtime-auto-combat.service.ts`
  - 已完成：`selectAutoCombatTarget()` 预计算一次怪物敌对关系，并在同轮筛选中复用。
  - 已完成：候选 `metrics` 从两次 `reduce()` 改为遍历时单次累积。
  - 已完成：自动战斗技能选择改为一次构建技能索引并复用，避免每个 action 重复线性扫描 `player.techniques.techniques`。
  - 结果：减少自动战斗目标选择和技能选择路径中的重复对象构造、重复遍历和重复查找开销，不改变行为。
- [x] `packages/server/src/persistence/player-domain-persistence.service.ts`
  - 已完成：`replacePlayerInventoryItems()` 复用 `createPersistedInventoryRowSignature()`，避免对同一 `raw_payload` 做双重 `JSON.stringify()` 比较。
  - 已完成：同一批背包 `rows` 只生成一次完整 `rowsJson`，槽位清理、upsert 和最终清理共用同一份 JSON payload。
  - 已完成：`replacePlayerWalletRows()` 同一批 `normalizedRows` 只生成一次 `normalizedRowsJson`，upsert 和 cleanup 复用同一份 JSON payload。
  - 已完成：`replacePlayerEquipmentSlots()` 同一批装备槽 `rows` 只生成一次完整 `rowsJson`，upsert 和 cleanup 复用同一份 JSON payload。
  - 已完成：`replacePlayerTechniqueStates()` 同一批 `normalizedRows` 只生成一次完整 `normalizedRowsJson`，upsert 和 cleanup 复用同一份 JSON payload。
  - 结果：减少 player domain 各类分域写入中的重复序列化、临时数组和临时字符串分配，不改变行为。
- [x] `packages/server/src/persistence/player-persistence.service.ts`
  - 已完成：`normalizeRuntimeBonuses()` 改为单次循环构建 runtime bonus 结果，并在同一轮里规范化 `qiProjection`。
  - 结果：减少玩家快照运行态 bonus 规范化中的重复遍历和临时数组分配，不改变行为。
- [x] `packages/server/src/persistence/outbox-dispatcher.service.ts`
  - 已完成：提取 topic prefix 规范化 helper，`claimReadyEvents()` 和 `listRetryQueue()` 复用同一批 `LIKE` pattern。
  - 结果：减少出站队列筛选入口的重复遍历和临时数组分配，不改变行为。
- [x] `packages/server/src/persistence/player-identity-persistence.service.ts`
  - 已完成：提取玩家 ID 规范化 helper，一次完成 trim、过滤和去重，三个身份查找入口复用。
  - 结果：减少身份查找路径中的重复遍历和临时数组分配，不改变行为。
- [x] `packages/server/src/persistence/compat/item-instance-id-compat.ts`
  - 已完成：`upsertEquipmentSlotRowsWithItemInstanceIdRepair()` 每次 repair 后只生成一次完整 `rowsJson`，删除冲突行和 upsert 共用同一份 JSON payload。
  - 结果：减少装备分域落库重试路径中的重复 `map()`、重复序列化和临时字符串分配，不改变行为。
- [x] `packages/server/src/persistence/durable-operation.service.ts`
  - 已完成：`replacePlayerInventoryItems()` 在 durable 流程中复用行签名，避免对同一 `raw_payload` 做双重 `JSON.stringify()` 比较。
  - 已完成：同一批 durable 背包 `rows` 只生成一次完整 `rowsJson`，槽位清理、upsert 和最终清理共用同一份 JSON payload。
  - 已完成：`replacePlayerMarketStorageItems()` 同一批 durable 市场仓库 `rows` 只生成一次完整 `rowsJson`，冲突槽位清理、upsert 和最终清理共用同一份 JSON payload。
  - 已完成：durable 钱包替换路径同一批 `rows` 只生成一次完整 `rowsJson`，upsert 和 cleanup 共用同一份 JSON payload。
  - 结果：减少 durable 背包、市场仓库和钱包替换中的重复序列化、临时数组和临时字符串分配，不改变行为。
- [x] `packages/server/src/persistence/instance-domain-persistence.service.ts`
  - 已完成：建筑、建筑格子、房间、房间格子、风水五类快照各自只生成一次完整 JSON payload，upsert 和 cleanup 共用同一份 JSON。
  - 已完成：`saveInstanceContainerStates()` 预生成 `containerRowsJson`、`containerIdsJson`、`timerRowsJson`、`entryRowsJson`、`entryKeysJson`，插入和清理查询复用同一批 payload。
  - 已完成：`saveTileResourceDiffs()` 在规范化资源条目时同步构建写入 rows 与清理 key rows，并复用预生成 JSON。
  - 已完成：`saveTileResourceDeltaBatch()` 在规范化批量 upsert/delete 时直接生成 JSON payload，避免后续循环再次 map。
  - 结果：减少实例分域保存中的重复 `map()`、重复序列化和临时字符串分配，不改变行为。
- [x] `packages/server/src/persistence/mail-persistence.service.ts`
  - 已完成：`pruneStructuredMailboxSnapshot()` 预先生成 `mailIdsJson` 和 `attachmentIdsJson`，多个删除语句复用同一份 key-only payload。
  - 结果：减少邮件分域清理路径中的重复 `map()`、重复序列化和临时字符串分配，不改变行为。
- [x] `packages/server/src/persistence/market-persistence.service.ts`
  - 已完成：`persistStructuredStorages()` 预先生成 `slotIndicesJson`，cleanup 复用同一份 payload。
  - 结果：减少市场仓库写入中的重复 `map()` 和临时字符串分配，不改变行为。
