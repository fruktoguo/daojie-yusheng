# 前端问题清单

> 目标：持续扫描 `packages/client` 前端所有可见 bug、性能问题、全量刷新打断、只展示 ID/元数据而未展示中文名或真实数据的问题。
> 规则：能直接修复的进入「已处理问题」并随修复提交；需要继续实现的进入「待处理问题」；需要产品/运维口径选择的进入「待决定问题」。

---

## 待处理问题

### F-008 [P1] 邮件附件协议缺少服务端展示名，未知模板只能显示「未知物品」

- 文件：`packages/shared/src/mail-types.ts:7-19`、`packages/client/src/ui/mail-panel.ts`、`packages/client/src/react-ui/panels/mail/MailPanel.tsx`
- 现象：`MailAttachment` 只有 `itemId/count`，当前前端只能依赖本地模板补中文名；模板缺失时无法还原服务端真实名称。
- 影响：无法完全满足「真实数据/正确中文名」要求，尤其是临时物品、活动物品、配置不同步或 GM 邮件附件。
- 下一步：协议层给附件补稳定展示名或 item template revision，服务端组包时从权威模板填充，客户端继续以本地模板作校验/兜底。

---

## 已处理问题

### F-060 [P1] 市场面板本地占位物品缺少模板时直接显示 `itemId`

- 文件：`packages/client/src/ui/panels/market-panel.ts:2079-2089`
- 现象：市场面板在本地补齐预览物品时，模板缺失会直接把 `itemId` 当作名称。
- 影响：市场面板的占位物品会暴露内部物品 ID。
- 处理：已统一改为「未知物品」兜底。
- 验证：`pnpm verify:client` 通过（客户端构建与 production boundary check 均通过；仅保留 Vite/protobuf eval 与大 chunk 既有警告）。

### F-059 [P2] sect 管理按钮回调在缺少文案时直接显示 `actionId`

- 文件：`packages/client/src/ui/panels/action-panel.ts:3138-3144`
- 现象：sect 管理按钮回调在按钮文案缺失时，会把 `actionId` 继续传给上层，当作动作名使用。
- 影响：sect 管理相关动作链路会暴露内部动作 ID。
- 处理：已统一改为「未知行动」兜底。
- 验证：`pnpm verify:client` 通过（客户端构建与 production boundary check 均通过；仅保留 Vite/protobuf eval 与大 chunk 既有警告）。

### F-058 [P1] 自动用药 buff 摘要缺失名称时直接显示 `buffId`

- 文件：`packages/client/src/ui/panels/action-panel.ts:4109-4113`、`packages/client/src/ui/panels/action-panel-combat-settings.ts:515-518`
- 现象：自动用药配置的效果摘要里，buff 名缺失时会直接把 `buffId` 拼进去。
- 影响：自动用药配置摘要会暴露内部 buff ID。
- 处理：已统一改为「未知状态效果」兜底。
- 验证：`pnpm verify:client` 通过（客户端构建与 production boundary check 均通过；仅保留 Vite/protobuf eval 与大 chunk 既有警告）。

### F-057 [P1] 工坊引导文案缺失成品名时直接显示 `outputItemId`

- 文件：`packages/client/src/ui/craft-workbench-modal.ts:1304-1310`、`packages/client/src/ui/craft-alchemy-view.ts:1402-1410`
- 现象：工坊炼器入门列表和炼丹子标题在缺少成品名时，会直接把 `outputItemId` 当作展示文本。
- 影响：工坊引导文案会暴露内部物品 ID。
- 处理：已统一改为「未知物品」兜底。
- 验证：`pnpm verify:client` 通过（客户端构建与 production boundary check 均通过；仅保留 Vite/protobuf eval 与大 chunk 既有警告）。

### F-056 [P1] 工坊队列成品名缺失时直接显示 `outputItemId`

- 文件：`packages/client/src/ui/craft-queue-view.ts:125-132`
- 现象：工坊队列在炼丹作业缺少成品名时，会直接把 `outputItemId` 当作名称显示。
- 影响：工坊队列会暴露内部物品 ID。
- 处理：已统一改为「未知物品」兜底。
- 验证：`pnpm verify:client` 通过（客户端构建与 production boundary check 均通过；仅保留 Vite/protobuf eval 与大 chunk 既有警告）。

### F-053 [P2] GM 地图编辑器实体标签在缺少名字时直接显示 `id`

- 文件：`packages/client/src/gm-map-editor.ts:1559-1563,1595-1599,1782-1786,1819-1823,2131-2156,3696-3704,3728-3758`
- 现象：GM 地图编辑器在 NPC、怪物、地标、容器等实体名称缺失时，原先会直接把对应 `id` 画进列表、画布标注和侧边摘要里。
- 影响：编辑器会暴露内部 ID。
- 处理：已统一改为「未命名场景人物 / 未命名怪物 / 未命名地标 / 未命名容器」兜底。
- 验证：`pnpm verify:client` 通过（客户端构建与 production boundary check 均通过；仅保留 Vite/protobuf eval 与大 chunk 既有警告）。

### F-021 [P0] 客户端浏览器 worker 路径导致世界显示和动画不一致

- 文件：`packages/client/src/game-map/renderer/canvas-text-renderer-adapter.ts`、`packages/client/src/pathfinding.ts`、`packages/client/src/workers/*`、`docs/plans/服务端与客户端多线程并行化改造计划.md`
- 现象：客户端多线程改造引入 OffscreenCanvas 渲染 worker、寻路 worker 和前端 worker 文档口径，渲染 worker 路径与主线程完整渲染器不等价。
- 影响：世界画面、角色动画、特效层级可能与主线程渲染表现不一致。
- 处理：移除客户端浏览器 worker 路径；客户端保持主线程 Canvas 渲染与同步寻路；计划文档改为服务端 worker 口径并明确禁止客户端 worker。
- 验证：`pnpm verify:client` 通过（客户端构建与 production boundary check 均通过；仅保留 Vite/protobuf eval 与大 chunk 既有警告）。

### F-020 [P1] 工坊面板与强化历史在模板缺失时直接显示 `itemId`

- 文件：`packages/client/src/ui/craft-workbench-modal.ts:93-116,585-591,721-774,888-889,3106-3112,3368-3381`、`packages/client/src/ui/craft-enhancement-view.ts:93-116,587-588,721-773,888-888,1361-1362,1420-1432`
- 现象：工坊/强化相关面板在缺少本地模板名时，会把 `itemId` 直接当作物品名展示在目标槽、列表和历史记录里。
- 影响：强化/打造界面会暴露内部物品 ID。
- 处理：已统一改为「未知物品」兜底。
- 验证：`pnpm verify:client` 通过（客户端构建与 production boundary check 均通过；仅保留 Vite/protobuf eval 与大 chunk 既有警告）。

### F-019 [P1] 离线收益面板物品名缺失时直接显示 `itemId`
- 文件：`packages/client/src/ui/offline-gain-render.ts:171-183`
- 现象：离线收益面板的物品收支列表在缺少本地模板名时，会直接把 `entry.itemId` 当作显示名。
- 影响：离线收益报告会暴露内部物品 ID。
- 处理：已统一改为「未知物品」兜底。
- 验证：`pnpm verify:client` 通过（客户端构建与 production boundary check 均通过；仅保留 Vite/protobuf eval 与大 chunk 既有警告）。

### F-018 [P1] 自动用药配置在模板缺失时直接显示 `itemId`
- 文件：`packages/client/src/ui/panels/action-panel.ts:2767-2784`、`packages/client/src/ui/panels/action-panel-combat-settings.ts:258-275`
- 现象：自动用药配置列表在草稿项没有本地模板名称时，会直接把 `config.itemId` 当作名称显示。
- 影响：战斗设置里的自动用药选择器会暴露内部物品 ID。
- 处理：已统一改为「未知物品」兜底。
- 验证：`pnpm verify:client` 通过（客户端构建与 production boundary check 均通过；仅保留 Vite/protobuf eval 与大 chunk 既有警告）。

### F-017 [P1] React external store 无变化 `patchState` 仍广播导致无意义重渲染

- 文件：`packages/client/src/react-ui/stores/create-external-store.ts:39-75`
- 现象：`patchState` 每次都创建新对象，`Object.is(state, nextState)` 永远为 false，导致即使 patch 值没变也会通知所有订阅者。
- 影响：React UI shell、面板 store 在高频同步场景下会产生不必要重渲染，形成潜在性能问题。
- 处理：已增加 patch 浅比较，只在实际字段变化时合并状态并广播。
- 验证：`pnpm verify:client` 通过（客户端构建与 production boundary check 均通过；仅保留 Vite/protobuf eval 与大 chunk 既有警告）。

### F-016 [P0] ContentResolver 动态 Buff 注入引用已移除字段导致客户端构建失败

- 文件：`packages/client/src/content/content-resolver.ts:286-303`
- 现象：`injectBuffs` 直接读取 `GmEditorBuffOption.valueStats`，但当前共享类型已不再声明该字段。
- 影响：`pnpm verify:client` 在 TypeScript 阶段失败，阻断前端构建验证。
- 处理：已改为通过 `in` 守卫读取 legacy `valueStats`，兼容旧数据但不再破坏当前类型。
- 验证：`pnpm verify:client` 通过（客户端构建与 production boundary check 均通过；仅保留 Vite/protobuf eval 与大 chunk 既有警告）。

### F-015 [P1] GM 地图编辑器地图目标标签仍回退显示 `mapId`

- 文件：`packages/client/src/gm-map-editor.ts:2202-2212`
- 现象：地图编辑器在找不到目标地图时，目标标签曾直接显示 `mapId`。
- 影响：GM 地图编辑器中会出现裸 ID 的目标地图文本。
- 处理：已改为「未知地域」兜底，已有名称时仍显示中文名 + ID。
- 验证：`pnpm verify:client` 通过（客户端构建与 production boundary check 均通过；仅保留 Vite/protobuf eval 与大 chunk 既有警告）。

### F-014 [P1] 静态 GM 工具地图摘要仍回退显示 `mapId`

- 文件：`packages/client/src/gm.ts:4982-4988,5042-5059`
- 现象：静态 GM 工具在缺少地图摘要时，会把 `mapId` 作为地图标签和下拉内容的兜底文本。
- 影响：GM 管理后台会出现只显示 ID 的地图信息，影响排障和人工筛选。
- 处理：已接入本地地图缓存元数据；无 meta 时统一改为「未知地域」兜底。
- 验证：`pnpm verify:client` 通过（客户端构建与 production boundary check 均通过；仅保留 Vite/protobuf eval 与大 chunk 既有警告）。

### F-013 [P1] GM 面板地图选择与玩家地图标签仍过度回退到 `mapId`

- 文件：`packages/client/src/ui/panels/gm-panel.ts:101-107,592-609`、`packages/client/src/react-ui/panels/gm/GmPanel.tsx:101-107,379-439`
- 现象：DOM/React GM 面板里的玩家地图标签和地图下拉在缺少/未命中 meta 时，曾直接展示裸 `mapId`。
- 影响：GM 工具中也会出现只有内部 ID 的地图信息，不利于排障与人工确认。
- 处理：已改为优先显示「中文名（ID）」；若无元数据则继续保留 ID 作为操作键。
- 验证：`pnpm verify:client` 通过（客户端构建与 production boundary check 均通过；仅保留 Vite/protobuf eval 与大 chunk 既有警告）。

### F-012 [P1] 小地图目录分组名仍回退显示 `mapId`

- 文件：`packages/client/src/ui/minimap.ts:473-483`
- 现象：小地图目录分组在缺少权威 map meta 时，`resolveMapGroupInfo` 的输入名称曾直接使用 `entry.mapId`。
- 影响：目录分组标题可能暴露内部地图 ID。
- 处理：已统一改为「未知地域」兜底。
- 验证：`pnpm verify:client` 通过（客户端构建与 production boundary check 均通过；仅保留 Vite/protobuf eval 与大 chunk 既有警告）。

### F-011 [P1] 装备条件地图标签仍回退显示 `mapId`

- 文件：`packages/client/src/ui/equipment-tooltip.ts:207-229`
- 现象：装备条件说明中的地图列表在缺少权威 map meta 时，曾把 `mapId` 作为兜底名称。
- 影响：装备 tooltip 可能暴露内部地图 ID。
- 处理：已统一改为「未知地域」兜底。
- 验证：`pnpm verify:client` 通过（客户端构建与 production boundary check 均通过；仅保留 Vite/protobuf eval 与大 chunk 既有警告）。

### F-010 [P1] 小地图兜底元数据仍显示内部 `mapId`

- 文件：`packages/client/src/ui/minimap.ts:449-470,1320-1323,1888-1901`
- 现象：小地图目录与删除记忆弹层在缺少权威地图 meta 时，曾把 `mapId` 作为兜底地图名或标题。
- 影响：小地图与记忆管理弹层可能暴露内部地图 ID。
- 处理：已将兜底名称统一改为「未知地域」，仅保留 ID 作为内部定位键。
- 验证：`pnpm verify:client` 通过（客户端构建与 production boundary check 均通过；仅保留 Vite/protobuf eval 与大 chunk 既有警告）。

### F-009 [P1] React HUD 与运行态桥接曾回退显示 `mapId`

- 文件：`packages/client/src/ui/hud.ts:224-248`、`packages/client/src/main-runtime-state-source.ts:386-406,647-660`、`packages/client/src/react-ui/app/ReactUiScaffold.tsx:23-45`
- 现象：React HUD 状态构建与 React UI 运行态 `mapName` 桥接曾存在 `player.mapId` 兜底；脚手架 mapLabel 也曾在无 `mapName` 时展示 `mapId`。
- 影响：地图 meta/进入包名称缺失时，React 主界面可能暴露内部地图 ID。
- 处理：已将 React HUD、运行态桥接和脚手架主显示兜底统一改为中文未知态；`mapId` 只保留在 title 排障信息中。
- 验证：`pnpm verify:client` 通过（客户端构建与 production boundary check 均通过；仅保留 Vite/protobuf eval 与大 chunk 既有警告）。

### F-007 [P1] 邮件附件未知模板时直接显示 `itemId`

- 文件：`packages/client/src/ui/mail-panel.ts:74-91,1042-1050,1164-1168`、`packages/client/src/react-ui/panels/mail/MailPanel.tsx:67-76,355-361`
- 现象：DOM/React 邮件详情附件列表在本地模板缺失时，直接把 `attachment.itemId` 显示为物品名。
- 影响：玩家邮件附件区域可能暴露内部物品 ID，命中「只显示 id 或元数据」要求。
- 处理：已统一改为玩家可读的「未知物品」兜底，悬浮提示也改为显示可读名称，不再把 `itemId` 作为可见文本或 title；本地模板存在时仍显示真实中文名。
- 验证：`pnpm verify:client` 通过（客户端构建与 production boundary check 均通过；仅保留 Vite/protobuf eval 与大 chunk 既有警告）。

### F-006 [P1] DOM 世界面板与 HUD 缺少地图 meta 时显示内部 `mapId`

- 文件：`packages/client/src/ui/panels/world-panel.ts:160-186`、`packages/client/src/main-ui-state-source.ts:279-287`、`packages/client/src/ui/hud.ts:149-165,224-248`
- 现象：主 DOM 世界面板/HUD 在地图 meta 尚未到达时，会把 `player.mapId` 作为地图名兜底。
- 影响：玩家主界面可能短暂或长期显示内部地图 ID，命中「只显示 id 或元数据而没有显示正确中文名字/真实数据」要求。
- 处理：已将玩家可见兜底统一改为「未知地域」；真实中文名仍优先来自地图运行态 `mapMeta.name`。
- 验证：`pnpm verify:client` 通过（客户端构建与 production boundary check 均通过；仅保留 Vite/protobuf eval 与大 chunk 既有警告）。

### F-005 [P1] React 世界面板缺少地图 meta 时显示内部 `mapId`

- 文件：`packages/client/src/react-ui/panels/world/WorldPanel.tsx:84-108`
- 现象：世界面板构建未知地图摘要时，若无 `WORLD_GUIDE` 且 `mapMeta` 未到达，会把 `player.mapId` 作为标题展示。
- 影响：玩家可见区域出现内部地图 ID，命中「只显示 id 或元数据而没有显示正确中文名字/真实数据」要求。
- 处理：已将玩家可见兜底标题改为「未知地域」，真实中文名仍优先来自 `mapMeta.name` 或静态 guide。
- 验证：`pnpm verify:client` 通过（客户端构建与 production boundary check 均通过；仅保留 Vite/protobuf eval 与大 chunk 既有警告）。

### F-004 [P1] QQ 加群入口会导航当前游戏页

- 文件：`packages/client/src/main-startup-bindings.ts:372-383,481-494`
- 现象：点击 QQ 加群入口后通过 `window.location.href = ...` 在当前页面跳转外部协议/链接。
- 影响：可能让当前游戏客户端离页，打断 socket、Canvas、输入与弹层上下文；属于外链场景的全量页面打断风险。
- 处理：已改为在用户点击激活窗口内使用 `window.open(..., '_blank', 'noopener,noreferrer')` 唤起 QQ，同时保留群号复制和被拦截提示，不再改写当前页 location。
- 验证：`pnpm verify:client` 通过（客户端构建与 production boundary check 均通过；仅保留 Vite/protobuf eval 与大 chunk 既有警告）。

### F-003 [P1] React 原型脚手架地图字段只显示 `mapId`

- 文件：`packages/client/src/react-ui/app/ReactUiScaffold.tsx:27-45`、`packages/client/src/ui/panel-system/types.ts:207-232`、`packages/client/src/main-runtime-state-source.ts:386-400,647-652`
- 现象：React UI 脚手架的「地图」字段读取 `shellState.runtime.mapId`，面向玩家/测试者时可能显示内部地图 ID。
- 影响：命中「只显示 id 或元数据而没有显示正确中文名字/真实数据」要求，且 React 渐进式 UI 会把旧 DOM 已处理过的中文地图名能力倒退成 ID 展示。
- 处理：已给面板运行态补 `mapName`，首包与地图静态同步从 `mapMeta.name`/进入包名称桥接中文名；React 展示与 hover 提示都优先使用 `mapName`，不再回退显示 `mapId`。
- 验证：`pnpm verify:client` 通过（客户端构建与 production boundary check 均通过；仅保留 Vite/protobuf eval 与大 chunk 既有警告）。


### F-049 [P1] GM 功法选择项缺失时直接显示 `techniqueId`

- 文件：`packages/client/src/gm.ts:1451-1455`
- 现象：GM 功法选择项在缺少名称时，会直接把 `techniqueId` 当作选项文本显示。
- 影响：GM 后台会出现只显示 ID 的功法选项。
- 处理：已统一改为「未知功法」兜底。
- 验证：`pnpm verify:client` 通过（客户端构建与 production boundary check 均通过；仅保留 Vite/protobuf eval 与大 chunk 既有警告）。

### F-051 [P1] 工坊强化保护物说明缺失时直接显示 `protectionItemId`

- 文件：`packages/client/src/ui/craft-workbench-modal.ts:2476-2478`
- 现象：工坊强化面板在保护物名称缺失时，会直接把 `protectionItemId` 显示在保护物说明里。
- 影响：工坊强化面板会暴露内部物品 ID。
- 处理：已统一改为「未知物品」兜底。
- 验证：`pnpm verify:client` 通过（客户端构建与 production boundary check 均通过；仅保留 Vite/protobuf eval 与大 chunk 既有警告）。

### F-052 [P1] 动作补丁合并缺失时直接显示 `patch.id`

- 文件：`packages/client/src/main-panel-delta-state-source.ts:845-853`
- 现象：动作状态增量合并在名称缺失时，会直接把 `patch.id` 当作动作名。
- 影响：玩家动作列表会暴露内部动作 ID。
- 处理：已统一改为「未知动作」兜底。
- 验证：`pnpm verify:client` 通过（客户端构建与 production boundary check 均通过；仅保留 Vite/protobuf eval 与大 chunk 既有警告）。

### F-050 [P1] 建筑/风水显示名缺失时直接显示 `id`

- 文件：`packages/client/src/main-building-fengshui-state-source.ts:376-385`
- 现象：建筑/风水相关显示名在缺少本地名称时，会直接把 `entry.id` 当作建筑名参与展示。
- 影响：建筑/风水相关 UI 会暴露内部建筑 ID。
- 处理：已统一改为「未命名建筑」兜底。
- 验证：`pnpm verify:client` 通过（客户端构建与 production boundary check 均通过；仅保留 Vite/protobuf eval 与大 chunk 既有警告）。

### F-048 [P1] 离线收益面板功法名缺失时直接显示 `techniqueId`

- 文件：`packages/client/src/ui/offline-gain-render.ts:140-145`
- 现象：离线收益面板的功法收支列表在缺少本地模板名时，会直接把 `techniqueId` 当作功法名显示。
- 影响：离线收益面板会暴露内部功法 ID。
- 处理：已统一改为「未知功法」兜底。
- 验证：`pnpm verify:client` 通过（客户端构建与 production boundary check 均通过；仅保留 Vite/protobuf eval 与大 chunk 既有警告）。

### F-046 [P1] sect 管理成员/申请人/宗主名字缺失时直接显示 `playerId`

- 文件：`packages/client/src/ui/panels/action-panel.ts:269-345`、`packages/client/src/ui/panels/action-panel-sect-management.ts:138-171`
- 现象：sect 管理界面在成员、申请人、当前宗主名称缺失时，会直接把 `playerId` 显示出来。
- 影响：sect 管理面板会暴露内部玩家 ID。
- 处理：已统一改为「未知成员 / 未知申请人 / 当前宗主」兜底，不再直接显示 `playerId`。
- 验证：`pnpm verify:client` 通过（客户端构建与 production boundary check 均通过；仅保留 Vite/protobuf eval 与大 chunk 既有警告）。

### F-045 [P1] 市场面板物品名缺失时直接显示 `itemId`

- 文件：`packages/client/src/ui/panels/market-panel.ts:2586-2592`
- 现象：市场面板在缺少物品名时，会把 `itemId` 当作物品名展示在列表、详情和订单相关面板里。
- 影响：市场面板会暴露内部物品 ID。
- 处理：已统一改为「未知物品」兜底。
- 验证：`pnpm verify:client` 通过（客户端构建与 production boundary check 均通过；仅保留 Vite/protobuf eval 与大 chunk 既有警告）。

### F-043 [P1] 实体详情弹窗 buff 来源缺失时直接显示 `sourceSkillId`

- 文件：`packages/client/src/ui/entity-detail-modal.ts:701-703`
- 现象：实体详情弹窗在 buff 来源技能名缺失时，会直接把 `sourceSkillId` 显示出来。
- 影响：实体详情弹窗会暴露内部技能 ID。
- 处理：已统一改为「未明」兜底，不再直接显示 `sourceSkillId`。
- 验证：`pnpm verify:client` 通过（客户端构建与 production boundary check 均通过；仅保留 Vite/protobuf eval 与大 chunk 既有警告）。

### F-042 [P1] sect 管理角色/权限标签缺失时直接显示 `id`

- 文件：`packages/client/src/ui/panels/action-panel.ts:300-330`、`packages/client/src/ui/panels/action-panel-sect-management.ts:74-104`
- 现象：sect 管理面板在角色/权限标签缺失时，会直接把内部 `id` 显示出来。
- 影响：sect 管理列表会暴露内部角色/权限 ID。
- 处理：已统一改为「未知角色 / 未知权限」兜底，不再直接显示 `id`。
- 验证：`pnpm verify:client` 通过（客户端构建与 production boundary check 均通过；仅保留 Vite/protobuf eval 与大 chunk 既有警告）。

### F-041 [P1] 观测 buff 来源缺失时直接显示 `sourceSkillId`

- 文件：`packages/client/src/main-observe-state-source.ts:582-584`
- 现象：观测 buff tooltip 在来源技能名缺失时，会直接把 `sourceSkillId` 显示给玩家。
- 影响：观测 buff tooltip 会暴露内部技能 ID。
- 处理：已统一改为使用「未知」兜底，不再直接显示 `sourceSkillId`。
- 验证：`pnpm verify:client` 通过（客户端构建与 production boundary check 均通过；仅保留 Vite/protobuf eval 与大 chunk 既有警告）。

### F-040 [P1] 动作名称缺失时直接显示 `action.id`

- 文件：`packages/client/src/main-runtime-state-source.ts:323-330`
- 现象：动作状态回填时在名称缺失时会直接把 `action.id` 作为动作名。
- 影响：动作面板可能把内部动作 ID 直接显示给玩家。
- 处理：已统一改为「未知动作」兜底，不再直接显示 `action.id`。
- 验证：`pnpm verify:client` 通过（客户端构建与 production boundary check 均通过；仅保留 Vite/protobuf eval 与大 chunk 既有警告）。

### F-039 [P1] 观测地块存在列表缺失实体名时直接显示 `entity.id`

- 文件：`packages/client/src/main-observe-state-source.ts:1234-1237`
- 现象：观测地块存在列表在实体名缺失时，会把 `entity.id` 传给实体种类标签函数并显示出来。
- 影响：观测面板地块存在列表会暴露内部实体 ID。
- 处理：已统一改为仅显示实体种类名，不再把 `entity.id` 作为兜底。
- 验证：`pnpm verify:client` 通过（客户端构建与 production boundary check 均通过；仅保留 Vite/protobuf eval 与大 chunk 既有警告）。

### F-038 [P1] 观测面板实体标题缺失时直接显示 `entity.id`

- 文件：`packages/client/src/main-observe-state-source.ts:970-974`
- 现象：观测面板实体标题在既无怪物展示名也无实体名时，会直接把 `entity.id` 显示出来。
- 影响：观测面板会暴露内部实体 ID。
- 处理：已统一改为「目标」兜底，不再直接显示 `entity.id`。
- 验证：`pnpm verify:client` 通过（客户端构建与 production boundary check 均通过；仅保留 Vite/protobuf eval 与大 chunk 既有警告）。

### F-037 [P1] 装备 tooltip 时间阶段缺失时直接显示 `phaseId`

- 文件：`packages/client/src/ui/equipment-tooltip.ts:202-205`
- 现象：装备 tooltip 在时间阶段标签缺失时，会直接把 `phaseId` 显示给玩家。
- 影响：装备 tooltip 会暴露内部时间阶段 ID。
- 处理：已统一改为「未知时辰」兜底，不再回退显示 `phaseId`。
- 验证：`pnpm verify:client` 通过（客户端构建与 production boundary check 均通过；仅保留 Vite/protobuf eval 与大 chunk 既有警告）。

### F-036 [P1] 聊天 buff 名缺失时回退显示 `buffId`

- 文件：`packages/client/src/ui/chat.ts:545-573`
- 现象：聊天战斗日志在 buff/debuff 名缺失时，会把 `buffId` 当成可见名称。
- 影响：战斗聊天里会暴露内部 buff ID。
- 处理：已统一改为优先 buff 模板名，再走「未知效果」兜底。
- 验证：`pnpm verify:client` 通过（客户端构建与 production boundary check 均通过；仅保留 Vite/protobuf eval 与大 chunk 既有警告）。

### F-035 [P1] 强化保护物与炼丹作业标题回退显示 `itemId`

- 文件：`packages/client/src/ui/craft-enhancement-view.ts:677-679`、`packages/client/src/ui/craft-workbench-modal.ts:1692-1697,2088-2090`
- 现象：强化保护物说明与炼丹作业标题/副标题在缺少中文名时，会直接暴露 `protectionItemId` / `outputItemId`。
- 影响：强化与炼丹面板会把内部物品 ID 当作展示名。
- 处理：已统一改为「未知物品」兜底，作业进度和数量保持不变。
- 验证：`pnpm verify:client` 通过（客户端构建与 production boundary check 均通过；仅保留 Vite/protobuf eval 与大 chunk 既有警告）。

### F-034 [P1] 炼丹作业材料名缺失时直接显示 `itemId`

- 文件：`packages/client/src/ui/craft-alchemy-view.ts:730-739`、`packages/client/src/ui/craft-workbench-modal.ts:2114-2123`
- 现象：炼丹作业流在材料模板缺失时，会把 `ingredient.itemId` 当作展示名。
- 影响：炼丹作业列表会暴露内部物品 ID。
- 处理：已统一改为「未知物品」兜底，材料流程仍保留数量与配方信息。
- 验证：`pnpm verify:client` 通过（客户端构建与 production boundary check 均通过；仅保留 Vite/protobuf eval 与大 chunk 既有警告）。

### F-033 [P1] 任务面板必需物品名缺失时直接显示 `itemId`

- 文件：`packages/client/src/ui/panels/quest-panel.ts:876-885`、`packages/client/src/ui/npc-quest-modal.ts:706-714`、`packages/client/src/react-ui/panels/quest/QuestPanel.tsx:105-109,341-349`
- 现象：任务详情、NPC 任务弹窗和 React 任务面板在必需物品缺少本地模板名时，会直接把 `quest.requiredItemId` 当作展示名。
- 影响：任务目标/任务需求文本会暴露内部物品 ID。
- 处理：已统一改为「未知物品」兜底，任务进度和说明仍保留真实数量与进度。
- 验证：`pnpm verify:client` 通过（客户端构建与 production boundary check 均通过；仅保留 Vite/protobuf eval 与大 chunk 既有警告）。

### F-032 [P1] 离线收益悬浮提示回退显示 `itemId`

- 文件：`packages/client/src/ui/offline-gain-render.ts:171-183`
- 现象：离线收益的物品行在 hover/title 里直接暴露 `itemId`。
- 影响：玩家鼠标悬停时会看到内部物品 ID。
- 处理：已移除 `title` 中的 `itemId`，只保留玩家可读名称或「未知物品」。
- 验证：`pnpm verify:client` 通过（客户端构建与 production boundary check 均通过；仅保留 Vite/protobuf eval 与大 chunk 既有警告）。

### F-031 [P1] 实体详情标题与字段回退显示玩家/实体 ID

- 文件：`packages/client/src/ui/entity-detail-modal.ts:249-373`
- 现象：实体详情在玩家、地面或无标题实体上，会回退展示 `player.id`、`ground.sourceId` 或 `detail.id`。
- 影响：玩家观测详情里会看到内部实体 ID 或来源 ID。
- 处理：已改为使用玩家/地面/通用「详情」等玩家可读标题，玩家栏位也不再展示裸 ID。
- 验证：`pnpm verify:client` 通过（客户端构建与 production boundary check 均通过；仅保留 Vite/protobuf eval 与大 chunk 既有警告）。

### F-030 [P1] 观察面板地面来源回退显示 `sourceId`

- 文件：`packages/client/src/main-observe-state-source.ts:1194-1375`
- 现象：观察弹窗的地面摘要和元信息会把 `ground.sourceId` 直接展示出来。
- 影响：玩家可见观察面板会暴露地面来源内部 ID。
- 处理：已改为仅显示地面物品数量摘要，不再输出来源 ID。
- 验证：`pnpm verify:client` 通过（客户端构建与 production boundary check 均通过；仅保留 Vite/protobuf eval 与大 chunk 既有警告）。

### F-029 [P1] 地面详情回退显示来源 `sourceId`

- 文件：`packages/client/src/ui/entity-detail-modal.ts:397-419`
- 现象：点击地面物品堆时，详情标题/副标题曾直接展示地面来源 ID。
- 影响：地面物品详情会暴露内部来源标识。
- 处理：已改为显示「地面物」与物品数量摘要，不再显示来源 ID。
- 验证：`pnpm verify:client` 通过（客户端构建与 production boundary check 均通过；仅保留 Vite/protobuf eval 与大 chunk 既有警告）。

### F-028 [P1] 内嵌妖兽标签与提示回退显示 `monsterId`

- 文件：`packages/client/src/ui/item-inline-tooltip.ts:124-217`
- 现象：妖兽 chip / tooltip 在缺少本地名称时，会把 `monsterId` 当成可见文本或标题。
- 影响：文本高亮、妖兽提示和悬浮说明会暴露内部妖兽 ID。
- 处理：已统一改为「未知妖兽」兜底，并优先用本地或权威名称。
- 验证：`pnpm verify:client` 通过（客户端构建与 production boundary check 均通过；仅保留 Vite/protobuf eval 与大 chunk 既有警告）。

### F-027 [P1] 同步功法名回退为 `techId`

- 文件：`packages/client/src/main-panel-delta-state-source.ts:568-580`
- 现象：功法同步数据在名称缺失时，会回退到 `techId`。
- 影响：功法面板可能直接显示内部功法 ID。
- 处理：已统一改为「未知功法」兜底。
- 验证：`pnpm verify:client` 通过（客户端构建与 production boundary check 均通过；仅保留 Vite/protobuf eval 与大 chunk 既有警告）。

### F-026 [P1] 传送详情回退显示目标地图 ID

- 文件：`packages/client/src/main-observe-state-source.ts:1270-1316`、`packages/client/src/ui/entity-detail-modal.ts:375-395`
- 现象：传送点/门派/楼梯详情缺少中文名时，会回退展示 `targetMapId`。
- 影响：玩家观测面板与实体详情会暴露目标地图内部 ID。
- 处理：已统一改为「未知地域」兜底，不再输出裸 ID。
- 验证：`pnpm verify:client` 通过（客户端构建与 production boundary check 均通过；仅保留 Vite/protobuf eval 与大 chunk 既有警告）。

### F-025 [P1] 自动用药 tooltip 回退显示 `itemId`

- 文件：`packages/client/src/ui/panels/action-panel.ts:2856-2873`、`packages/client/src/ui/panels/action-panel-combat-settings.ts:338-353`
- 现象：自动用药配置的悬浮说明在模板缺失时会回退到 `itemId`。
- 影响：战斗设置里的药品提示会暴露内部物品 ID。
- 处理：已统一改为「未知物品」兜底。
- 验证：`pnpm verify:client` 通过（客户端构建与 production boundary check 均通过；仅保留 Vite/protobuf eval 与大 chunk 既有警告）。

### F-024 [P1] 同步物品名回退为 `itemId`

- 文件：`packages/client/src/main-panel-delta-state-source.ts:589-600`
- 现象：物品同步数据在名称缺失时，会回退到 `itemId`。
- 影响：背包、地面、面板等所有消费同步物品名的 UI 都可能暴露内部物品 ID。
- 处理：已统一改为「未知物品」兜底。
- 验证：`pnpm verify:client` 通过（客户端构建与 production boundary check 均通过；仅保留 Vite/protobuf eval 与大 chunk 既有警告）。

### F-023 [P1] 内嵌物品标签回退显示 `itemId`

- 文件：`packages/client/src/ui/item-inline-tooltip.ts:113-217`
- 现象：内嵌物品 chip / tooltip 在模板缺失时，会把 `itemId` 直接作为可见文本或标题。
- 影响：任务文本、掉落展示、物品提示和高亮会暴露内部物品 ID。
- 处理：已统一改为「未知物品」兜底，并保留真正的 item 数据用于 tooltip。
- 验证：`pnpm verify:client` 通过（客户端构建与 production boundary check 均通过；仅保留 Vite/protobuf eval 与大 chunk 既有警告）。

### F-022 [P1] 炼丹面板在模板缺失时直接显示 `itemId`

- 文件：`packages/client/src/ui/craft-alchemy-view.ts:637-820`
- 现象：炼丹/炼器界面的物品引用在模板缺失时，会把 `itemId` 当作显示名。
- 影响：炼丹材料、辅助材料和数量行会暴露内部物品 ID。
- 处理：已借助统一的内嵌物品兜底逻辑改为「未知物品」。
- 验证：`pnpm verify:client` 通过（客户端构建与 production boundary check 均通过；仅保留 Vite/protobuf eval 与大 chunk 既有警告）。

### F-001 [P1] React 原型关闭按钮触发全页刷新打断

- 文件：`packages/client/src/react-ui/app/ReactUiScaffold.tsx:92-100`
- 现象：点击「关闭 React 脚手架」先写入 `__toggleMudReactUi__(false)`，随后执行 `window.location.reload()`。
- 影响：会断开 socket、重建 Canvas/DOM、丢失当前焦点/滚动/弹层/输入上下文，属于明确的前端全量刷新打断。
- 处理：已改为只切换 React UI feature flag，由 `shellStore` 驱动 `ReactUiRoot` 返回 `null`，不再刷新页面。
- 验证：`pnpm verify:client` 通过（客户端构建与 production boundary check 均通过；仅保留 Vite/protobuf eval 与大 chunk 既有警告）。

---

## 待决定问题

### F-002 [P0] 生产构建版本变化时仍会强制 `location.replace` 刷新

- 文件：`packages/client/src/version-reload.ts:168-172`、`packages/client/src/main-app-runtime-context.ts:60-62`、`packages/client/src/gm.ts:636-640`
- 现象：检测到新 `buildId` 后，客户端提示后延迟执行 `window.location.replace(...)`。
- 影响：部署时会打断在线玩家正在进行的移动、交易、阅读、输入、弹层选择等操作；GM 页面也会被强制刷新。
- 待决定：新版本策略应选择哪种口径：A. 仅提示并由用户手动刷新；B. 玩家空闲/不在关键操作时自动刷新；C. 保留当前强制刷新但加更明确的倒计时和可取消。
- 推荐默认：A，避免任何自动全量刷新打断，后续可补服务端兼容窗口和版本过期提示。

### F-044 [P2] GM 地图编辑器任务卡片摘要在缺少名字时仍回退显示 ID

- 文件：`packages/client/src/gm-map-editor-helpers.ts:173`
- 现象：任务卡片摘要在缺少目标名称时，会把 `targetNpcId`、`targetMonsterId` 或 `requiredItemId` 拼进摘要文本。
- 影响：GM 地图编辑器的任务卡片会直接暴露内部 ID，但同时也保留了精确定位所需的元数据。
- 待决定：是否保留 ID 便于编辑排障，还是统一改成「未知目标」并补充独立的名称字段。
- 推荐默认：先保留 ID，不改编辑器摘要口径；如果后续需要面向非技术角色，再单独补名称字段。
