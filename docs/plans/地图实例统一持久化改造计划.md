# 地图实例统一持久化改造计划

> 解决问题：所有地图实例（模板/秘境/宗门）统一持久化模型，tickSpeed 实例化，恢复流程由 instance_catalog 驱动。

## 状态：✅ 全部完成

所有 Phase 已完成并验证通过：

- [x] Phase 1：tickSpeed 迁移到实例级别
- [x] Phase 2：通天塔实例持久化
- [x] Phase 3：统一恢复流程（instance_catalog 驱动）
- [x] Phase 4：统一地块恢复接口（TileRecoveryProvider）
- [x] Phase 5：秘境框架预留（DungeonDescriptor）
- [x] Phase 6：废弃 `server_gm_map_config` 表

## 落地结果

- 所有实例 `persistent: true`，运行态完整落盘
- tickSpeed/paused 存入实例 checkpoint，按 instanceId 独立配置
- 启动恢复完全由 `instance_catalog` 驱动
- 动态模板（通天塔/宗门）恢复时按 `instance_type` 自动注册
- `server_gm_map_config` 表已废弃删除
- 秘境 `dungeonDescriptor` 预留扩展接口
