# 旧实现残留清理计划

## 背景

项目经历多轮重构（持久化分域、地图分层、通知结构化、战斗重构等），大量旧实现未及时删除，导致：
- 新开发者无法判断哪个是当前主线实现
- IDE 搜索和跳转命中废弃代码
- 旧代码与新代码共存增加维护负担和出错概率
- 部分旧代码仍被注入/导入但实际不执行任何有效逻辑

本计划基于 2025-05-12 全面排查结果，按优先级分阶段清理。

## 原则

- 只删除确认无活跃调用方或已被新实现完全替代的代码
- 有活跃调用方的旧兼容层按迁移计划推进，不强删
- 每阶段清理后执行对应验证门禁
- 不改变任何运行时行为，纯减法操作

---

## 阶段一：零风险立即删除

**验证门禁**：`pnpm build`

| # | 目标 | 说明 |
|---|------|------|
| 1 | 删除 `packages/server/src/persistence/player-persistence.types.ts` | 空文件 `export {}`，零引用 |
| 2 | 删除 `packages/server/src/persistence/map-persistence.types.ts` | 空文件 `export {}`，零引用 |
| 3 | 删除 `packages/shared/src/technique.ts` 中 `calcBodyTrainingAttrBonus` | @deprecated，全项目零调用 |
| 4 | 删除 `packages/shared/src/constants/gameplay/technique.ts` 中 `TECHNIQUE_EXP_TABLE` | 旧经验表，全项目零引用 |
| 5 | 删除根目录 `storage.json`、`playwright-market-text.json` | 临时测试产物 |
| 6 | 将 `test-results/`、`benchmarks/` 加入 `.gitignore` | 临时目录不应入库 |

---

## 阶段二：文档与配置修正

**验证门禁**：手动确认链接可达

| # | 目标 | 说明 |
|---|------|------|
| 7 | `README.md` 中 `verify:replace-ready:*` → `verify:release:*` | 命令已重命名 |
| 8 | `README.md` 中文档链接修正 | `docs/next-plan/` → `docs/archive/`，`docs/protocol-audit.md` → `docs/archive/protocol-audit.md` |
| 9 | `docs/archive/README.md` 移除不存在的 6 个文件条目 | 索引与实际不一致 |
| 10 | `.codex/skills/verification-author/SKILL.md` 命令修正 | 同 #7 |

---

## 阶段三：重复代码消除

**验证门禁**：`pnpm build` + `pnpm verify:client`

| # | 目标 | 说明 |
|---|------|------|
| 11 | `action-panel.ts` 和 `action-panel-combat-settings.ts` 中 `buildLegacyHostileTargetingFallback` 合并为一份 | 同一函数两处实现 |
| 12 | `craft-workbench-modal.ts` 和 `craft-enhancement-view.ts` 中旧 localStorage 读取逻辑提取为共享函数 | 同一逻辑两处实现 |
| 13 | `config-editor/src/main.ts` 中 `EQUIP_SLOT_LABELS`、`TECHNIQUE_CATEGORY_LABELS` 改为从 shared 导入 | 与 shared 完全重复 |
| 14 | `config-editor/local-api.cjs` 中 `TECHNIQUE_GRADES`、`ITEM_TYPES`、`TECHNIQUE_CATEGORIES`、`MONSTER_AGGRO_MODES` 改为从 shared 导入 | 与 shared 完全重复 |

---

## 阶段四：废弃服务与空壳清理

**验证门禁**：`pnpm verify:quick` + `pnpm verify:release:with-db`

| # | 目标 | 说明 |
|---|------|------|
| 15 | `MapPersistenceService` 中规范化工具函数迁移至迁移工具后删除整个服务文件 | 所有方法已空实现/throw |
| 16 | `WorldPlayerSourceService` 从 DI 移除 | 所有方法已拦截，仅输出日志 |
| 17 | `packages/shared/src/types.ts` 兼容聚合口删除 | 改 3 处内部导入为直接导入后删除 |
| 18 | `config-editor/index.html` 旧 valueStats hidden section + `main.ts` 空渲染函数删除 | 已停用 |
| 19 | `config-editor/local-api.cjs` 中 `normalizeMonsterValueStats` 重命名为 `normalizeEquipValueStats` | 命名误导 |

---

## 阶段五：旧通知链路迁移（配合现有计划）

**关联文档**：[旧文本通知链路删除计划](旧文本通知链路删除计划.md)

**验证门禁**：`pnpm verify:quick` + 客户端聊天面板手动验证

| # | 目标 | 说明 |
|---|------|------|
| 20 | 服务端 14 个文件约 30 处 `queuePlayerNotice` 纯文本 → 结构化 | 按现有计划 P1-P4 顺序推进 |
| 21 | 客户端 `chat.ts:518` 旧文本 fallback 正则删除 | 战斗系统结构化完成后 |

**涉及文件**：
- `world-runtime-npc-quest-write.service.ts`（7处）
- `world-runtime-loot-container.service.ts`（4处）
- `world-runtime-equipment.service.ts`（4处）
- `world-runtime-use-item.service.ts`（3处）
- `world-runtime-craft-mutation.service.ts`（2处）
- `world-runtime-npc-shop.service.ts`（2处）
- `world-runtime-cultivation.service.ts`、`world-runtime-item-ground.service.ts`、`world-runtime-respawn.service.ts`、`world-runtime-navigation.service.ts`、`world-runtime-sect.service.ts`、`world-runtime-building.service.ts`、`world-runtime-action-execution.service.ts`、`world-runtime-system-command.service.ts`（各1处）
- `player-progression.service.ts`（14处 `text:` 字段）

---

## 阶段六：旧格式兼容层退役（需数据迁移）

**前置条件**：确认生产数据已全部迁移到新格式

**验证门禁**：`pnpm verify:release:full`

| # | 目标 | 说明 |
|---|------|------|
| 22 | 功法 `attrCurves` 旧格式字段 + `legacyCurves` 参数路径删除 | 需确认所有功法模板已迁移到新格式 |
| 23 | 战斗目标旧布尔字段 `includeNormalMonsters` 等删除 | 需确认所有玩家存档已迁移 |
| 24 | `Tile.type`（旧 TileType）删除，统一使用分层字段 | 需确认所有地图数据已迁移 |
| 25 | `map-layer-types.ts` 旧 TileType 兼容转换层删除 | 同上 |
| 26 | `aura.ts` 旧灵气等级值转换函数删除 | 需确认所有地图灵气配置已迁移 |
| 27 | `offline-gain-types.ts` 旧字段 `count`/`amount`/`expGain` 删除 | 需确认客户端不再读取旧字段 |
| 28 | `notice-types.ts` 旧 `text` 字段删除 | 阶段五全部完成后 |
| 29 | `network-protobuf.ts` 空壳函数填充或删除 | 等 Protobuf 正式启用决策 |

---

## 阶段七：一次性工具与技术债

**验证门禁**：`pnpm build:server`

| # | 目标 | 说明 |
|---|------|------|
| 30 | 评估 `import-legacy-persistence-once.ts` 是否仍需保留 | 一次性迁移脚本 |
| 31 | 评估 `convert-main-dump-to-current-db.ts` 是否仍需保留 | 一次性转换工具 |
| 32 | `scripts/` 中 5 个未注册脚本评估删除 | `migrate-monster-tendencies.mjs` 等 |
| 33 | server 旧身份来源 `legacy_sync`/`legacy_backfill` 分支清理 | 主线已拒绝 |
| 34 | server 旧战斗服务瘦身/删除 | 配合战斗重构计划阶段 14 |
| 35 | GM 面板 `toLegacyPlayerState` / `LegacyRuntimeTileInput` 适配新结构 | 低优先级 |
| 36 | `config-editor/local-api.cjs` 逐步迁移为 TS | 44KB CJS 文件 |
| 37 | `packages/server/src/tools/` 180 个 `@ts-nocheck` 文件逐步迁移 | 长期技术债 |

---

## 不动项（设计如此，暂不清理）

| 项目 | 原因 |
|------|------|
| `docker-compose.shadow.yml` | shadow 验证专用，端口和密码隔离有实际意义 |
| `LEGACY_BACKUP_FILE_KIND` 等旧备份常量 | 需要识别和恢复历史备份文件 |
| `LEGACY_MAP_TERRAIN_PROFILE_IDS` | 运行时仍在使用，等地图分层迁移完成 |
| 玩家属性旧字段兼容 `comprehension→strength` | 运行时仍在使用，等存档全量迁移 |
| `chat-storage.ts` 旧 localStorage 清理逻辑 | 一次性迁移用，对已有玩家仍有价值 |
| `config-editor` 怪物三种 sourceMode | 旧数据只读兼容，等数据全量迁移到 tendency |

---

## 执行节奏

- **阶段一、二**：立即执行，无风险
- **阶段三**：本周内完成
- **阶段四**：本周内完成，需验证 DI 和构建
- **阶段五**：按旧文本通知链路删除计划节奏，持续推进
- **阶段六**：等各自前置数据迁移完成后逐项执行
- **阶段七**：低优先级，穿插在日常开发中逐步推进
