# 商业级数据落盘改造计划

> 基于 [mmo商业级数据落盘方案.md](../mmo商业级数据落盘方案.md) 的终局设计，结合当前 next 主线代码现状制定。

---

## 当前落地快照（2026-04-23）

当前计划已经进入实施态，但还没有完成到终局。

已落地：

- `PlayerDomainPersistenceService` 已接入玩家快照双写投影，当前已经落到 `player_presence`、`player_world_anchor`、`player_position_checkpoint`、`player_vitals`、`player_progression_core`、`player_attr_state`、`player_body_training_state`、`player_inventory_item`、`player_map_unlock`、`player_equipment_slot`、`player_technique_state`、`player_persistent_buff_state`、`player_quest_progress`、`player_combat_preferences`、`player_auto_battle_skill`、`player_auto_use_item_rule`、`player_profession_state`、`player_alchemy_preset`、`player_active_job`、`player_enhancement_record`、`player_logbook_message`、`player_recovery_watermark`
- `PlayerDomainPersistenceService` 现已对已落地投影子集暴露单域直写接口：`savePlayerWorldAnchor / savePlayerPositionCheckpoint / savePlayerVitals / savePlayerProgressionCore / savePlayerAttrState / savePlayerInventoryItems / savePlayerMapUnlocks / savePlayerEquipmentSlots / savePlayerTechniques / savePlayerBodyTraining / savePlayerQuests / savePlayerCombatPreferences / savePlayerAutoBattleSkills / savePlayerAutoUseItemRules / savePlayerBuffs / savePlayerProfessionState / savePlayerAlchemyPresets / savePlayerActiveJob / savePlayerEnhancementRecords / savePlayerLogbookMessages`，并在单域事务内推进对应 `player_recovery_watermark`
- `player_vitals`、`player_progression_core`、`player_attr_state`、`player_body_training_state`、`player_persistent_buff_state` 与 `player_enhancement_record` 已开始承接 next 主线写入；当前已落地字段子集分别是 `hp/max_hp/qi/max_qi`、`foundation/combat_exp/bone_age_base_years/life_elapsed_ticks/lifespan_years`、`base_attrs_payload/bonus_entries_payload/revealed_breakthrough_requirement_ids/realm_payload/heaven_gate_payload/spiritual_roots_payload`、`level/exp/exp_to_next`、`buff_id/source_skill_id/source_caster_id/realm_lv/remaining_ticks/duration/stacks/max_stacks/sustain_ticks_elapsed/raw_payload` 与 `record_id/item_id/highest_level/levels_payload/action_started_at/action_ended_at/start_level/initial_target_level/desired_target_level/protection_start_level/status`
- `PlayerDomainPersistenceService.loadPlayerDomains()` 与 `loadProjectedSnapshot()` 已落地；当前在旧 `server_next_player_snapshot` 缺失时，可以从已落地分域投影子集回组装玩家快照，现已覆盖 `inventory / map unlock / equipment / technique / persistent buff / quest / combat config / attr / enhancement record`
- `WorldPlayerSnapshotService` 已接入“旧快照 miss -> 分域投影恢复”读链；对外仍保持 `source=next`，通过 `fallbackReason+=player_domain_projection` 标记分域投影来源，且 `player_presence` 单独存在时不会被误当成可恢复主档
- `player_presence.runtime_owner_id + session_epoch` 已接入玩家会话 fencing 基线
- `DurableOperationService` 已落地第一条强持久化事务链：邮件附件领取
- `durable_operation_log`、`outbox_event`、`asset_audit_log` 已建表并接入邮件附件领取
- 邮件主链已从“纯 mailbox JSON”推进到 `player_mail / player_mail_attachment / player_mail_counter` 结构化真源，`persistent_documents(scope=server_next_mailboxes_v1)` 只保留兼容镜像
- `MailRuntimeService` 的 `markRead / claim / delete / createDirectMail / ensureWelcomeMail` 已开始按受影响邮件局部 upsert 结构化真源，不再每次都整箱删光 `player_mail*` 后重写
- `MarketPersistenceService` 已把玩家坊市托管仓落到 `player_market_storage_item` 结构化真源，当前运行时启动与 restore 后重载都会优先从该表回读；`persistent_documents(scope=server_market_storage_v1)` 只保留兼容镜像
- GM 数据库备份/恢复已升级到 `mainline_persistence` 口径，覆盖 `persistent_documents` 与已落地的 next 结构化主线表；当前备份载荷已显式覆盖 `player_map_unlock / player_market_storage_item / player_equipment_slot / player_technique_state / player_persistent_buff_state / player_quest_progress / player_combat_preferences / player_auto_battle_skill / player_auto_use_item_rule / player_attr_state / player_enhancement_record`
- `gm-database` destructive smoke 已补到值级断言：当前会在 restore 后验证 `player_presence / player_vitals / player_progression_core / player_attr_state / player_body_training_state / player_persistent_buff_state / player_enhancement_record / player_market_storage_item / player_mail / player_mail_attachment / player_mail_counter` 的结构化回滚结果，并保留“备份前结构化邮件仍保留、备份后新增邮件会回滚消失”的双向证明；GM 认证回滚也已加入备份/恢复断言
- `player-domain-persistence-smoke`、`player-domain-recovery-smoke` 与 `gm-database-smoke` 已在 2026-04-23 再次通过；其中 `gm-database-smoke` 现已覆盖 `player_attr_state / player_persistent_buff_state / player_enhancement_record / player_market_storage_item` 的 restore 后值级回滚断言，并保留 interrupted-restore 在 `creating_pre_import_backup` 阶段被强制重启后应落为 failed 的证明
- `persistence-smoke` 已补成直接 proof：当前会先删除 compat 文档，再证明 `player_market_storage_item` 在服务重启后仍能被运行时真实回读到 `/runtime/players/:playerId/market`，并完成 `claim-storage` 领取与表内清空
- `pnpm --filter @mud/server verify:proof:with-db` 已在 2026-04-23 再次通过；当前 with-db proof 已覆盖 auth/bootstrap 主证明链、基础持久化 smoke、玩家分域投影/恢复、邮件强事务与本地 destructive restore
- `persistence-smoke / gm-database-smoke / gm-database-backup-persistence-smoke` 已切到稳定 `dist` 快照起服，避免 restore 或多次重启窗口误读共享 `dist`
- `pnpm verify:replace-ready:with-db` 已通过，当前 with-db 自动化证明链已覆盖玩家分域投影、邮件强事务、本地 destructive restore 与协议审计；仍不等于 shadow / acceptance / full 环境证明
- `durable-operation-smoke` 已从“只测 DurableOperationService 本体”推进到同时覆盖 `MailRuntimeService.claimAttachments()` 的真实领取入口
- `migrate-next-mainline-once --domains=player-domain` 已落地，当前覆盖已落地投影子集：`player_world_anchor / player_position_checkpoint / player_vitals / player_progression_core / player_body_training_state / player_inventory_item / player_profession_state / player_alchemy_preset / player_active_job / player_logbook_message / player_recovery_watermark`

仍未落地：

- `instance_catalog`、`instance_overlay_chunk`、`node_registry`、lease/fencing、`flush_ledger`、独立 outbox dispatcher
- 玩家域分表还没有全部替代旧 `server_next_player_snapshot`
- 旧 `PlayerPersistenceFlushService` 仍然承担主快照兜底刷盘，域级 dirty + 多 worker 还未完整接管
- 地图实例仍以旧文档快照为主，尚未完成实例分域真源收口

## 现状诊断

### 当前持久化架构

| 数据域 | 当前存储 | 问题 |
|---|---|---|
| **玩家全量** | `server_next_player_snapshot` 单表，`payload` JSONB 整档写入 | 写放大极高，改一格背包重写整档 |
| **玩家身份** | `server_next_player_identity` 专表 | ✅ 已拆分 |
| **地图实例** | `persistent_documents` 通用表，`scope=server_next_map_aura_v1`，整包 JSON | 写放大高，无法按域恢复 |
| **邮件** | `player_mail / player_mail_attachment / player_mail_counter` + `persistent_documents` 兼容镜像 | 结构化真源已落地，但商业级批量投递与完整运营链未完成 |
| **坊市** | `persistent_documents`，三个 scope，已有事务 | 缺幂等，缺审计 |
| **兑换码/建议** | `persistent_documents` | 低频，暂可保留 |

### 当前 Flush 架构

| 组件 | 参数 | 问题 |
|---|---|---|
| `PlayerPersistenceFlushService` | 5s 间隔，batch 24，并行 4 | 整档写，无分域 dirty |
| `MapPersistenceFlushService` | 5s 间隔，batch 16，并行 3 | 整包写，无分域 dirty |

### 当前 Dirty 追踪

- 玩家：`persistentRevision > persistedRevision`，全局单一 revision，无域级标记
- 地图：`instance.isPersistentDirty()` + container dirty set，无域级标记

### 关键缺失

- ❌ 玩家数据分域专表（wallet/inventory/equipment/location/vitals 全在一个 JSON）
- ❌ 实例目录真源（`instance_catalog`）
- ❌ 实例状态分域（tile resource/ground item/container/overlay 全在一个 JSON）
- ⚠️ 强持久化命令链已起步（邮件附件领取已接入 `durable_operation_log` / `outbox_event` / `operation_id`），但未覆盖全部资产域
- ❌ 域级 dirty 追踪
- ❌ 多 flush worker
- ❌ 恢复水位（`player_recovery_watermark` / `instance_recovery_watermark`）
- ❌ 实例 lease / fencing
- ❌ 资产审计日志
- ❌ 分布式节点注册与发现（多节点部署的前提）
- ❌ 玩家会话路由（哪个节点拥有哪个玩家）
- ❌ 跨节点玩家转移协议
- ❌ 数据库连接池管理（当前每个 service 各自 `new Pool()`，多节点下连接数爆炸）
- ❌ Outbox 消费者架构（多节点下谁消费 outbox）
- ⚠️ 邮件分域拆表已起步（结构化真源与附件领取强事务已落地），但广播投递和运营回执体系未完成
- ❌ 容量规划与压测基准

---

## 阶段 1：止血 — 玩家数据分域拆表

> 目标：消除玩家整档 JSON 写放大，让位置、背包、装备、货币不再整档重写。

### 1.0 表结构口径

- [ ] 玩家域默认使用“列式主字段 + 局部 `JSONB` 细节字段”口径，不再设计“每张表一个大 JSON”
- [ ] 主键、唯一键、索引键、事务校验键、热点筛选字段必须直接建列
- [ ] `raw_payload` / `detail_jsonb` 只用于兼容未知旧结构、低频细节、形状易变子结构，不承担主真源全部语义

### 1.1 新建玩家分域专表

- [ ] 设计并创建 `player_identity` 表（`player_id, user_id, role_name, created_at, updated_at`）
- [ ] 设计并创建 `player_presence` 表（`player_id, online, in_world, last_heartbeat_at, offline_since_at, runtime_owner_id, session_epoch, transfer_state, transfer_target_node_id, updated_at`）
- [ ] 设计并创建 `player_world_anchor` 表（`player_id, respawn_template_id, respawn_instance_id, respawn_x, respawn_y, last_safe_template_id, last_safe_instance_id, last_safe_x, last_safe_y, last_transfer_at, updated_at`）
- [ ] 设计并创建 `player_position_checkpoint` 表（`player_id, instance_id, x, y, facing, checkpoint_kind, updated_at`）
- [x] 设计并创建 `player_vitals` 表；当前已落地字段子集为 `player_id, hp, max_hp, qi, max_qi, updated_at`，`dead` 待 next 真源补齐后再独立收口
- [x] 设计并创建 `player_progression_core` 表；当前已落地字段子集为 `player_id, foundation, combat_exp, bone_age_base_years, life_elapsed_ticks, lifespan_years, updated_at`，击杀计数与跨图导航冷却待 next 真源补齐后再接入
- [x] 设计并创建 `player_attr_state` 表；当前已落地字段子集为 `player_id, base_attrs_payload, bonus_entries_payload, revealed_breakthrough_requirement_ids, realm_payload, heaven_gate_payload, spiritual_roots_payload, updated_at`
- [ ] 设计并创建 `player_wallet` 表（`player_id, wallet_type, balance, frozen_balance, version, updated_at`）
- [x] 设计并创建 `player_inventory_item` 表；当前已落地字段子集为 `item_instance_id, player_id, slot_index, item_id, count, raw_payload, updated_at`
- [x] 设计并创建 `player_market_storage_item` 表；当前已落地字段子集为 `storage_item_id, player_id, slot_index, item_id, count, enhance_level, raw_payload, updated_at`，由 `MarketPersistenceService` 读写并已纳入 GM 备份恢复覆盖
- [x] 设计并创建 `player_equipment_slot` 表；当前已落地字段子集为 `player_id, slot_type, item_instance_id, item_id, raw_payload, updated_at`
- [x] 设计并创建 `player_technique_state` 表；当前已落地字段子集为 `player_id, tech_id, level, exp, exp_to_next, realm_lv, skills_enabled, raw_payload, updated_at`
- [x] 设计并创建 `player_body_training_state` 表；当前已落地字段子集为 `player_id, level, exp, exp_to_next, updated_at`
- [x] 设计并创建 `player_persistent_buff_state` 表；当前已落地字段子集为 `player_id, buff_id, source_skill_id, source_caster_id, realm_lv, remaining_ticks, duration, stacks, max_stacks, sustain_ticks_elapsed, raw_payload, updated_at`，只存跨重启保留的 buff
- [x] 设计并创建 `player_quest_progress` 表；当前已落地字段子集为 `player_id, quest_id, status, progress_payload, raw_payload, updated_at`
- [x] 设计并创建 `player_map_unlock` 表；当前已落地字段子集为 `player_id, map_id, unlocked_at, updated_at`
- [x] 设计并创建 `player_combat_preferences` 表；当前已落地字段子集为 `player_id, auto_battle, auto_retaliate, auto_battle_stationary, auto_battle_targeting_mode, retaliate_player_target_id, combat_target_id, combat_target_locked, allow_aoe_player_hit, auto_idle_cultivation, auto_switch_cultivation, sense_qi_active, cultivating_tech_id, targeting_rules_payload, updated_at`
- [x] 设计并创建 `player_auto_battle_skill` 表；当前已落地字段子集为 `player_id, skill_id, enabled, skill_enabled, auto_battle_order, updated_at`
- [x] 设计并创建 `player_auto_use_item_rule` 表；当前已落地字段子集为 `player_id, item_id, condition_payload, updated_at`
- [ ] 设计并创建 `player_profession_state` 表（`player_id, profession_type, level, exp, exp_to_next, updated_at`）
- [ ] 设计并创建 `player_alchemy_preset` 表（`preset_id, player_id, recipe_id, name, ingredients_payload, updated_at`）
- [ ] 设计并创建 `player_active_job` 表（`player_id, job_run_id, job_type, status, phase, started_at, finished_at, paused_ticks, total_ticks, remaining_ticks, success_rate, speed_rate, job_version, detail_jsonb, updated_at`）— 同一玩家只允许一个活跃技艺 job；`job_run_id` 是本次作业实例身份，`job_version` 在同一 `job_run_id` 内单调递增，所有暂停/继续/完成/取消都必须带 `job_run_id + job_version` 做 CAS；`detail_jsonb` 按 `job_type` 存真实差异字段：炼丹存 `recipe/output/ingredients/batch_progress`，强化存 `target/materials/protection/current_level/target_level`
- [x] 设计并创建 `player_enhancement_record` 表；当前已落地字段子集为 `record_id, player_id, item_id, highest_level, levels_payload, action_started_at, action_ended_at, start_level, initial_target_level, desired_target_level, protection_start_level, status, updated_at`
- [ ] 设计并创建 `player_logbook_message` 表（`message_id, player_id, kind, text, from_name, occurred_at, acked_at`）
- [ ] 建立所有必要索引

### 1.2 分域 Persistence Service

- [x] 新建 `PlayerDomainPersistenceService`，当前已包含已落地投影子集的 load/save 与投影回组装能力
- [x] `loadPlayerDomains(playerId)` — 当前已支持读取已落地投影子集（`player_world_anchor / player_position_checkpoint / player_vitals / player_progression_core / player_body_training_state / player_inventory_item / player_map_unlock / player_equipment_slot / player_profession_state / player_alchemy_preset / player_active_job / player_logbook_message / player_recovery_watermark`）
- [x] `loadProjectedSnapshot(playerId, buildStarterSnapshot)` — 当前已支持把已落地投影子集回组装成玩家快照，用于旧快照缺失时的恢复
- [ ] `savePlayerPresence(playerId, data)` — 单域写入
- [x] `savePlayerWorldAnchor(playerId, data)` — 单域写入
- [x] `savePlayerPositionCheckpoint(playerId, data)` — 单域写入
- [x] `savePlayerVitals(playerId, data)` — 单域写入
- [x] `savePlayerProgressionCore(playerId, data)` — 单域写入
- [x] `savePlayerAttrState(playerId, data)` — 单域写入
- [ ] `savePlayerWallet(playerId, data)` — 单域写入
- [x] `savePlayerInventoryItems(playerId, data)` — 单域写入
- [ ] `savePlayerMarketStorageItems(playerId, data)` — 单域写入
- [x] `savePlayerEquipmentSlots(playerId, data)` — 单域写入
- [x] `savePlayerTechniques(playerId, data)` — 单域写入
- [x] `savePlayerBodyTraining(playerId, data)` — 单域写入
- [x] `savePlayerQuests(playerId, data)` — 单域写入
- [x] `savePlayerMapUnlocks(playerId, data)` — 单域写入
- [x] `savePlayerCombatPreferences(playerId, data)` — 单域写入
- [x] `savePlayerAutoBattleSkills(playerId, data)` — 单域写入
- [x] `savePlayerAutoUseItemRules(playerId, data)` — 单域写入
- [x] `savePlayerBuffs(playerId, data)` — 单域写入
- [x] `savePlayerProfessionState(playerId, data)` — 单域写入
- [x] `savePlayerAlchemyPresets(playerId, data)` — 单域写入
- [x] `savePlayerActiveJob(playerId, data)` — 单域写入
- [x] `savePlayerEnhancementRecords(playerId, data)` — 单域写入
- [x] `savePlayerLogbookMessages(playerId, data)` — 单域写入

### 1.3 域级 Dirty 追踪

- [ ] 在 `PlayerRuntimeService` 中引入域级 dirty 标记（`dirtyDomains: Set<PlayerDomain>`）
- [ ] 每次状态变更只标记对应域为 dirty（`world_anchor / position_checkpoint / vitals / progression / attr / wallet / inventory / market_storage / equipment / technique / body_training / buff / quest / map_unlock / combat_pref / auto_battle_skill / auto_use_item_rule / profession / alchemy_preset / active_job / enhancement_record / logbook`）
- [ ] `listDirtyPlayers()` 改为 `listDirtyPlayerDomains()` 返回 `Map<playerId, Set<domain>>`

### 1.4 分域 Flush Worker

- [ ] `player_presence` 改为登录/掉线/心跳节流即时小事务，不走普通 flush worker
- [ ] 新建 `PlayerAnchorCheckpointFlushWorker`（1s 间隔，batch 256）— 处理 `player_world_anchor / player_position_checkpoint`
- [ ] 新建 `PlayerStateFlushWorker`（2-5s 间隔，batch 128）— 处理 `vitals / progression / attr / technique / body_training / quest / buff / combat_pref / profession / active_job(仅运行中进度字段)`
- [ ] `player_map_unlock / player_auto_battle_skill / player_auto_use_item_rule / player_alchemy_preset / player_logbook_message` 改为改动即写或短 debounce 小事务
- [ ] `player_enhancement_record` 改为强化结果结算时写入，不走高频 flush worker
- [ ] `player_wallet / player_inventory_item / player_market_storage_item / player_equipment_slot` 不进入普通 flush worker，统一走强持久化事务链
- [ ] 旧 `PlayerPersistenceFlushService` 降级为低频兜底 checkpoint（30s+）

### 1.5 分域恢复

- [ ] `PlayerRuntimeService.hydrateFromSnapshot` 改为从分域表加载并组装
- [x] `WorldPlayerSnapshotService` 已接入“旧快照 miss -> 分域投影恢复”读链，当前覆盖已落地投影子集
- [x] `player_presence` 单域不会单独触发恢复，避免把在线态误判为正式玩家档
- [ ] 保留从旧 `server_next_player_snapshot` 的 fallback 恢复路径
- [ ] 新玩家首次登录时同时写入分域表

### 1.6 数据迁移

- [x] 编写一次性迁移入口：`migrate-next-mainline-once --domains=player-domain`，当前把旧 `server_next_player_snapshot.payload` 投影到已落地子集（`player_world_anchor / player_position_checkpoint / player_vitals / player_progression_core / player_body_training_state / player_inventory_item / player_profession_state / player_alchemy_preset / player_active_job / player_logbook_message / player_recovery_watermark`）；`player_presence` 不在当前迁移目标内
- [ ] 编写一次性迁移脚本：从 `server_next_player_snapshot.payload` 拆分到 `player_identity / player_presence / player_world_anchor / player_position_checkpoint / player_vitals / player_progression_core / player_attr_state / player_wallet / player_inventory_item / player_market_storage_item / player_equipment_slot / player_technique_state / player_body_training_state / player_persistent_buff_state / player_quest_progress / player_map_unlock / player_combat_preferences / player_auto_battle_skill / player_auto_use_item_rule / player_profession_state / player_alchemy_preset / player_active_job / player_enhancement_record / player_logbook_message`
- [ ] 迁移后保留旧快照表作为兜底 checkpoint，不删除

### 1.7 验证

- [ ] 验证分域写入与旧整档写入数据一致性
- [ ] 验证分域恢复与旧快照恢复结果一致
- [x] 当前已落地投影子集已补 `player-domain-persistence-smoke + player-domain-recovery-smoke` 对照证明：旧整档快照写入后，投影子集的 `placement / vitals / progression core / attr / body training / inventory / map unlock / equipment / technique / persistent buff / quest / combat config / profession / preset / active_job / enhancement record / logbook` 与分域恢复结果保持一致
- [x] 新增 `player-domain-recovery-smoke`，验证“旧快照缺失 -> 分域投影恢复成功 / presence-only 不恢复”，且已覆盖 `vitals + progression core + body training`
- [x] 验证 `migrate-next-mainline-once --domains=player-domain --dry-run` 可读取旧快照并投影已落地子集
- [ ] 确认 `pnpm build` 通过
- [ ] 确认 `pnpm verify:replace-ready:doctor` 通过

---

## 阶段 1.5：强资产命令链

> 目标：货币、背包、装备等资产操作不再依赖异步 flush，走即时事务提交。

### 1.5.1 命令链基础设施

- [ ] 设计并创建 `durable_operation_log` 表（`operation_id, operation_type, aggregate_type, aggregate_id, player_id, runtime_owner_id, session_epoch, request_id, payload_jsonb, status, error_code, created_at, committed_at`）
- [ ] 设计并创建 `outbox_event` 表（`event_id, operation_id, topic, partition_key, payload_jsonb, status, attempt_count, next_retry_at, claimed_by, claim_until, created_at, delivered_at`）
- [ ] 设计并创建 `asset_audit_log` 表（`log_id, operation_id, player_id, asset_type, asset_ref_id, action, delta_jsonb, before_jsonb, after_jsonb, created_at`）
- [ ] 设计并创建 `player_recovery_watermark` 表（`player_id, identity_version, presence_version, anchor_version, position_checkpoint_version, vitals_version, progression_version, attr_version, wallet_version, inventory_version, market_storage_version, equipment_version, technique_version, body_training_version, buff_version, quest_version, map_unlock_version, combat_pref_version, auto_battle_skill_version, auto_use_item_rule_version, profession_version, alchemy_preset_version, active_job_version, enhancement_record_version, logbook_version, mail_version, mail_counter_version, updated_at`）
- [ ] 建立幂等唯一键索引

### 1.5.2 强持久化事务路径

- [ ] 实现 `DurableOperationService`，提供 `executeAssetMutation(operationId, expectedRuntimeOwnerId, expectedSessionEpoch, mutation)`
- [ ] 所有玩家强持久化事务先在事务内校验 `player_presence.runtime_owner_id = $expectedOwnerId AND player_presence.session_epoch = $expectedSessionEpoch`，旧连接、旧节点、顶号前旧 session 一律提交失败
- [ ] 货币增减 → 在同一事务中写 `durable_operation_log` + 修改 `player_wallet` + 更新 `player_recovery_watermark`
- [ ] 背包变更 → 在同一事务中写 `durable_operation_log` + 修改 `player_inventory_item / player_market_storage_item` + 更新 watermark
- [ ] 装备穿卸 → 在同一事务中写 `durable_operation_log` + 修改 `player_equipment_slot` + `player_inventory_item` + 更新 watermark
- [ ] 技艺作业创建/取消/完成 → 在同一事务中写 `durable_operation_log` + 按 `player_id + job_run_id + job_version` CAS 修改 `player_active_job` + 必要时修改 `player_wallet / player_inventory_item / player_enhancement_record` + 更新 watermark
- [ ] 事务提交成功后才返回成功结果

### 1.5.3 运行时集成

- [ ] `WorldRuntimeService` 中资产操作流程改为：tick 内判定 → 产出 mutation intent → 生成 `operation_id` 并携带当前 `runtime_owner_id + session_epoch` → 调用 `DurableOperationService` → 成功后更新内存态
- [ ] 邮件附件领取走强持久化路径
- [ ] 坊市成交走强持久化路径
- [ ] 顶号、跨节点转移、新会话接管时先 bump `session_epoch`，旧会话后续所有事务自动被 fencing 拒绝

### 1.5.4 验证

- [ ] 验证资产操作幂等性（重复 operation_id 不重复生效）
- [ ] 验证事务失败不影响运行时状态
- [x] `durable-operation-smoke` 已补“runtime 真实领取入口被 session fencing 拒绝后，邮箱摘要与内存背包保持不变”的直接证明，当前覆盖邮件附件领取事务失败不污染运行时状态
- [ ] 验证 watermark 正确推进
- [ ] 验证旧 session / 旧节点提交被拒绝
- [ ] 验证旧 `job_run_id` 或旧 `job_version` 的延迟完成不会覆盖新作业

---

## 阶段 2：实例目录化

> 目标：引入 `instance_catalog`，支持大量实例的恢复和查询。

### 2.1 实例目录表

- [ ] 设计并创建 `instance_catalog` 表：
  - 基础字段：instance_id, template_id, instance_type, persistent_policy
  - 归属字段：owner_player_id, owner_sect_id, party_id, line_id
  - 状态字段：status, runtime_status
  - 分布式字段：assigned_node_id, lease_token, lease_expire_at, ownership_epoch, cluster_id, shard_key, route_domain
  - 时间字段：created_at, last_active_at, last_persisted_at
- [ ] 建立 status/runtime_status 复合索引
- [ ] 建立 assigned_node_id + lease_expire_at 索引（节点故障扫描用）
- [ ] 建立 shard_key 索引（分布式路由用）

### 2.2 实例注册与查询

- [ ] 新建 `InstanceCatalogService` — 实例创建/查询/状态变更
- [ ] 实例创建时注册到 catalog
- [ ] 实例销毁时更新 catalog 状态
- [ ] 玩家实例内落点正式绑定 `player_position_checkpoint.instance_id`，长期恢复锚点留在 `player_world_anchor`

### 2.3 启动恢复改造

- [ ] 启动阶段改为先恢复 `instance_catalog`，再按策略决定哪些实例需要加载
- [ ] `permanent` 实例立即恢复
- [ ] `long_lived` 按 TTL 判断
- [ ] `session` / `ephemeral` 不恢复

### 2.4 玩家落点查询

- [ ] 玩家重连时通过 `instance_catalog` 查找实例状态
- [ ] 实例不存在或已销毁时转入 fallback 出生点

### 2.5 验证

- [ ] 验证实例创建/销毁正确更新 catalog
- [ ] 验证重启后按策略恢复正确
- [ ] 验证玩家 fallback 恢复正确

---

## 阶段 2.5：分布式基础设施 — Lease、路由与跨节点协调

> 目标：为多节点部署建立完整的所有权、路由和协调基础。这是从单服走向分布式的关键阶段。

### 2.5.1 节点注册与发现

- [ ] 设计并创建 `node_registry` 表（node_id, address, port, status, heartbeat_at, started_at, capacity_weight）
- [ ] 实现节点启动自注册
- [ ] 实现节点心跳（10-30s 周期）
- [ ] 实现节点故障检测（心跳超时 → 标记 suspect → 超时 → 标记 dead）
- [ ] 实现节点优雅下线（drain players → release leases → deregister）

### 2.5.2 实例 Lease 机制

- [ ] 实现 CAS（compare-and-swap）认领逻辑：`UPDATE instance_catalog SET assigned_node_id=$1, lease_token=$2, ownership_epoch=ownership_epoch+1 WHERE instance_id=$3 AND (assigned_node_id IS NULL OR lease_expire_at < now())`
- [ ] 实现周期性续约（lease TTL 建议 30-60s，续约周期 10-20s）
- [ ] 续约失败则停止写入并下线实例
- [ ] 节点故障后，其他节点可通过 lease 过期自动接管

### 2.5.3 Fencing

- [ ] 所有实例状态写入必须附带 `ownership_epoch`
- [ ] 数据库侧校验：`WHERE ownership_epoch = $expected_epoch`，旧 epoch 写入自动失败
- [ ] 运行时 fencing guard：写入前校验本节点 lease 是否仍有效

### 2.5.4 玩家会话路由

- [ ] 设计并创建 `player_session_route` 表或 Redis key（`player_id, node_id, session_epoch, route_status, updated_at`）
- [ ] 玩家登录时注册路由：`player:{playerId}:route → node_id + session_epoch`
- [ ] 玩家下线时清除路由
- [ ] Gateway 层根据路由将 socket 连接导向正确节点
- [ ] 路由不存在时分配到负载最低的节点

### 2.5.5 跨节点玩家转移

- [ ] 定义跨节点转移协议：
  1. 源节点把 `player_presence.transfer_state` 置为 `in_transfer`，并在事务内 bump `session_epoch`
  2. 源节点 flush 玩家所有 dirty 域与强持久化待提交操作
  3. 源节点写 `player_position_checkpoint` 到目标实例，并按需要更新 `player_world_anchor`
  4. 源节点写入新 `player_session_route(node_id, session_epoch)`，旧路由失效
  5. 目标节点按新 `session_epoch` 从分域表恢复玩家
  6. 目标节点确认接管后，源节点清除旧内存态并结束 transfer
- [ ] 实现转移超时与失败回滚
- [ ] 实现转移期间消息缓冲（避免丢包）

### 2.5.6 Outbox 消费架构

- [ ] 采用独立 `outbox-dispatcher` worker 集群消费 `outbox_event`，不让游戏节点各自消费自己产出的事件
- [ ] dispatcher 通过 `FOR UPDATE SKIP LOCKED` 或 CAS 认领 `status=ready AND next_retry_at <= now()` 的事件，并写入 `claimed_by + claim_until`
- [ ] 消费成功后标记 `status=delivered`
- [ ] 消费失败按退避重试，超过阈值进入 dead-letter
- [ ] 多节点下保证幂等消费（下游消费者按 `event_id` 或 `operation_id` 去重）
- [ ] worker 崩溃后通过 `claim_until` 超时回收事件

### 2.5.7 Split-brain 防护

- [ ] 旧节点 lease 过期后，必须立即停止所有该实例的内存写入
- [ ] 定期扫描本节点持有的实例 lease，与数据库 catalog 对账
- [ ] 发现 lease 被其他节点抢占时，立即放弃本地实例
- [ ] 所有强持久化事务在提交时二次校验 lease 有效性

### 2.5.8 数据库连接池统一管理

- [ ] 抽取统一的 `DatabasePoolProvider`，替代当前各 service 各自 `new Pool()` 的模式
- [ ] 配置连接池参数：单节点建议 `max=20-30`，多节点通过 PgBouncer 收敛
- [ ] 引入 PgBouncer 或 Supavisor 做连接复用（4 节点 × 30 连接 = 120，需要 pooler 控制总连接数）

### 2.5.9 验证

- [ ] 验证 lease 认领与续约流程
- [ ] 验证 fencing 阻止过期写入
- [ ] 验证节点故障后实例自动接管
- [ ] 验证跨节点玩家转移正确性
- [ ] 验证 split-brain 场景下不会出现双写

---

## 阶段 3：实例状态分域拆表

> 目标：去掉 `persistent_documents` 中整包地图快照依赖，实现实例状态分域。

### 3.1 实例状态专表

- [ ] 设计并创建 `instance_tile_resource_state` 表（instance_id, tile_index, resource_key, value）— 只存 diff
- [ ] 设计并创建 `instance_ground_item` 表（ground_item_id, instance_id, tile_index, item_instance_payload, expire_at）
- [ ] 设计并创建 `instance_container_state` 表（instance_id, container_id, source_id, state_payload）
- [ ] 设计并创建 `instance_monster_runtime_state` 表 — 只给高价值怪物
- [ ] 设计并创建 `instance_event_state` 表
- [ ] 设计并创建 `instance_overlay_chunk` 表（`instance_id, patch_kind, chunk_key, patch_version, patch_payload, updated_at`）— `patch_kind` 至少覆盖 `tile / portal / npc / container / rule`，高频 tile patch 以 chunk 维度拆分，全局 patch 走固定 `chunk_key`
- [ ] 设计并创建 `instance_recovery_watermark` 表
- [ ] 设计并创建 `instance_checkpoint` 表 — 仅冷启动加速用
- [ ] 建立所有必要索引

### 3.2 实例分域 Persistence Service

- [ ] 新建 `InstanceDomainPersistenceService`
- [ ] 实现 tile resource diff 写入（只写被改过的格子）
- [ ] 实现 ground item 单条写入/删除
- [ ] 实现 container state 单容器写入
- [ ] 实现高价值怪物状态写入
- [ ] 实现 overlay chunk / 子域写入

### 3.3 实例域级 Dirty 追踪

- [ ] `MapInstanceRuntime` 引入域级 dirty 标记
- [ ] `isPersistentDirty()` 拆为 `getDirtyDomains(): Set<InstanceDomain>`

### 3.4 实例分域恢复

- [ ] 恢复流程改为：catalog → watermark → 按域加载 → 组装运行时
- [ ] 普通怪物从模板 + spawn rule 重建，不从数据库加载

### 3.5 数据迁移

- [ ] 编写迁移脚本：从 `persistent_documents` 中 `server_next_map_aura_v1` 拆分到各域表
- [ ] 保留旧快照作为兜底

### 3.6 验证

- [ ] 验证分域写入与旧整包写入数据一致性
- [ ] 验证分域恢复结果一致
- [ ] 验证 diff 写入只写改过的格子

---

## 阶段 4：Worker 化

> 目标：把 flush 从服务逻辑里抽成专门调度层，多 worker 并行。

### 4.1 多 Worker 架构

- [ ] 新建 `PlayerAnchorCheckpointFlushWorker`（batch 256，1s）— 如阶段 1 未完成则在此补齐
- [ ] 新建 `PlayerStateFlushWorker`（batch 128，2-5s）
- [ ] 新建 `InstanceResourceFlushWorker`（batch 256，2s）
- [ ] 新建 `InstanceGroundItemFlushWorker`（batch 128，1-2s）
- [ ] 新建 `InstanceContainerFlushWorker`（batch 64，1-3s）
- [ ] 新建 `CheckpointCompactionWorker`（低峰期执行）

### 4.2 Flush Ledger + Redis 唤醒

- [ ] 设计并创建 `player_flush_ledger` 表（`player_id, domain, latest_version, flushed_version, dirty_since_at, next_attempt_at, claimed_by, claim_until, updated_at`）
- [ ] 设计并创建 `instance_flush_ledger` 表（`instance_id, domain, ownership_epoch, latest_version, flushed_version, dirty_since_at, next_attempt_at, claimed_by, claim_until, updated_at`）
- [ ] 运行时域变更时，先推进该域正式版本，再 upsert 对应 ledger 的 `latest_version`
- [ ] Worker 只从数据库 ledger 认领 `latest_version > flushed_version` 的条目；Redis 只做唤醒提示，不承担 dirty 真源
- [ ] Redis key 仅用于加速唤醒，例如 `flush:wakeup:player`、`flush:wakeup:instance`
- [ ] Redis 配置 Sentinel 或 Cluster 模式保证 HA
- [ ] Redis 故障时 worker 继续轮询数据库 ledger，最多损失唤醒速度，不丢 dirty 语义

### 4.3 登录风暴处理

- [ ] 玩家批量恢复使用并行加载 + 限流（避免重启后上千玩家同时恢复拖垮数据库）
- [ ] 实现恢复队列：按优先级排序（VIP / 最近活跃 / 普通）
- [ ] 恢复并行度可配置（建议初始值 32-64 并发）
- [ ] 恢复超时降级：超时玩家转入 fallback 出生点

### 4.4 失败重试

- [ ] 实现 retry 队列
- [ ] 失败事件进入 dead-letter 队列
- [ ] 新建 `dead_letter_event` 表

### 4.5 验证

- [ ] 压测多 worker 并行刷盘稳定性
- [ ] 验证 retry 和 dead-letter 流程
- [ ] 验证 flush ledger 与 Redis 唤醒解耦后仍可正确恢复待刷语义

---

## 阶段 4.5：物理层收口

> 目标：真正支撑大型 MMO 的数据规模。

### 4.5.1 分区策略

- [ ] `player_inventory_item` 按 `hash(player_id)` 分区
- [ ] `player_market_storage_item` 按 `hash(player_id)` 分区
- [ ] `player_wallet` 按 `hash(player_id)` 分区
- [ ] `player_active_job` 按 `hash(player_id)` 分区
- [ ] `player_mail` / `player_mail_attachment` 按 `hash(player_id)` 或 `hash(mail_id)` 分区
- [ ] `player_flush_ledger` 按 `hash(player_id)` 分区
- [ ] `instance_ground_item` 按 `hash(instance_id)` 分区
- [ ] `instance_tile_resource_state` 按 `hash(instance_id)` 分区
- [ ] `instance_overlay_chunk` 按 `hash(instance_id)` 分区
- [ ] `instance_flush_ledger` 按 `hash(instance_id)` 分区
- [ ] `durable_operation_log` / `outbox_event` / `asset_audit_log` 按 `created_at` 月分区

### 4.5.2 TTL 与清理

- [ ] 地面掉落物 TTL 到期异步清理
- [ ] destroyed / expired 实例先归档 catalog 再批量清理子表
- [ ] 过期邮件先标记 `expired`，再异步归档/清理附件与计数聚合
- [ ] 审计日志热表保留 7-30 天，冷数据归档

### 4.5.3 膨胀控制

- [ ] 批量 upsert 使用稳定顺序
- [ ] 优先 soft-delete + 周期清理
- [ ] 归档与 vacuum 运维窗口

### 4.5.4 验证

- [ ] 验证分区查询性能
- [ ] 验证 TTL 清理正确性

---

## 阶段 5：清理旧快照主链 + 邮件分域

> 目标：大快照不再是正式真源；邮件系统支持单封操作和强持久化附件领取。

### 5.1 旧快照退役

- [ ] `server_next_player_snapshot` 改为 checkpoint / backup 用途，不再作为恢复主链
- [ ] `persistent_documents` 中 `server_next_map_aura_v1` 改为兼容恢复或下线
- [ ] 移除 `PlayerPersistenceService` 中的旧快照恢复逻辑（保留 checkpoint 生成）
- [ ] 移除 `MapPersistenceService` 中的旧快照恢复逻辑
- [ ] 更新所有 smoke / verify / replace-ready 门禁

### 5.2 邮件分域拆表

- [ ] 设计并创建 `player_mail` 表（`mail_id, player_id, sender_type, sender_label, template_id, mail_type, title, body, source_type, source_ref_id, metadata_jsonb, mail_version, created_at, expire_at, read_at, claimed_at, deleted_at`）
- [ ] 设计并创建 `player_mail_attachment` 表（`attachment_id, mail_id, attachment_kind, item_id, count, currency_type, amount, item_payload_jsonb, claim_operation_id, claimed_at`）
- [ ] 设计并创建 `player_mail_counter` 表（`player_id, unread_count, unclaimed_count, latest_mail_at, counter_version, updated_at`）
- [ ] 邮件附件领取走强持久化事务（`durable_operation_log` + `player_mail_attachment` + `player_mail.claimed_at/mail_version` + `player_mail_counter` + `player_inventory_item / player_wallet` + `player_recovery_watermark`）
- [ ] 邮件已读/删除也推进 `player_mail.mail_version` 与 `player_mail_counter.counter_version`
- [ ] 编写迁移脚本：从 `persistent_documents` 中 `server_next_mailboxes_v1` 拆分
- [ ] 退役旧邮件 JSON 存储

---

## 阶段 6：观测与运维

> 目标：落盘系统可观测、可干预。

### 6.1 指标

- [ ] 每个 worker 每秒写入量
- [ ] 每个 worker 队列积压量
- [ ] 平均/最大 flush 延迟
- [ ] 强持久化事务提交耗时与失败率
- [ ] outbox / dead-letter backlog
- [ ] lease 冲突/接管次数
- [ ] 玩家 session fencing 拒绝次数
- [ ] mail counter 与 mail 主表不一致修复次数
- [ ] 单玩家/单实例恢复耗时
- [ ] 脏玩家/脏实例数

### 6.2 运维工具

- [ ] GM 命令：强制刷单玩家
- [ ] GM 命令：强制刷单实例
- [ ] GM 命令：强制重建某实例
- [ ] GM 命令：重放单个 `operation_id`
- [ ] GM 命令：查询实例 lease / owner
- [ ] GM 命令：冻结/解冻实例写入
- [ ] GM 命令：查看 dirty backlog
- [ ] GM 命令：查看失败重试队列
- [ ] GM 命令：查询节点列表与健康状态
- [ ] GM 命令：手动迁移实例到指定节点
- [ ] GM 命令：手动迁移玩家到指定节点

---

## 阶段 7：容量规划与压测

> 目标：验证方案在终局规模下可行。

### 7.1 数据量估算

| 表 | 估算行数（1000 玩家 + 1000 地图） | 说明 |
|---|---|---|
| `player_identity` | 1,000 | 1 玩家 1 行 |
| `player_position_checkpoint` | 1,000 | 1 玩家 1 行 |
| `player_inventory_item` | 50,000 - 100,000 | 1 玩家 50-100 物品 |
| `player_market_storage_item` | 5,000 - 20,000 | 托管仓或市场缓存物品 |
| `player_wallet` | 2,000 - 5,000 | 1 玩家 2-5 种货币 |
| `player_active_job` | 0 - 1,000 | 同时有活跃 job 的玩家子集 |
| `player_mail` | 10,000 - 200,000 | 取决于邮件保留期与运营活动密度 |
| `instance_catalog` | 1,000 - 5,000 | 含分线和动态实例 |
| `instance_tile_resource_state` | 100,000 - 1,000,000 | 1 实例平均 100-1000 个被改格子 |
| `instance_ground_item` | 10,000 - 100,000 | 取决于掉落密度和 TTL |
| `instance_overlay_chunk` | 10,000 - 200,000 | 洞府/门禁/改图按 chunk 或子域累积 |
| `durable_operation_log` | 按日增长，需月分区 | 每次资产操作 1 行 |

### 7.2 写入吞吐量估算

| 域 | 峰值写入频率 | 说明 |
|---|---|---|
| `player_world_anchor / player_position_checkpoint` | ~10-50 TPS | 登出、跨图、死亡回点、GM 传送，不再按每步移动落库 |
| `player_vitals / player_progression_core / player_body_training_state` | ~200-500 TPS | 战斗/修炼中的玩家 |
| `instance_tile_resource` | ~100-500 TPS | 采集/消耗 |
| `instance_ground_item` | ~50-200 TPS | 掉落/拾取 |
| 强持久化事务 | ~50-200 TPS | 交易/装备/货币 |

### 7.3 连接池规划

- [ ] 单节点 PG 连接池上限：20-30
- [ ] 多节点时引入 PgBouncer：总连接数控制在 100-200
- [ ] Redis 连接池：单节点 10-20，多节点 Sentinel/Cluster
- [ ] 连接池参数写入配置文件，不硬编码

### 7.4 压测计划

- [ ] 基准压测：500 玩家 + 500 地图，验证分域 flush 延迟 < 100ms
- [ ] 峰值压测：1000 玩家 + 1000 地图，验证刷盘不堵塞 tick
- [ ] 登录风暴：500 玩家同时重连，验证恢复队列工作正常
- [ ] 强持久化压测：100 TPS 资产操作，验证事务延迟 < 50ms
- [ ] 故障注入：kill 一个节点，验证 lease 接管 < 60s
- [ ] 跨节点转移压测：100 玩家同时跨节点传送

### 7.5 降级策略

- [ ] 数据库响应慢时：自动降低最终一致 flush 频率，优先保证强持久化事务和 lease 续约
- [ ] 数据库不可用时：暂停新强持久化资产操作并把世界切入受限模式，不承诺本地 WAL 降级；本地磁盘日志如需引入，必须单独立项设计幂等与重放
- [ ] Redis 不可用时：worker 直接轮询 flush ledger，保留写入正确性
- [ ] 单个 worker 积压时：告警但不阻塞其他 worker

---

## 实施优先级与依赖关系

```
阶段 1（止血）──→ 阶段 1.5（强资产）──→ 阶段 5（清理旧快照+邮件分域）
     │                    │
     └───→ 阶段 2（目录化）──→ 阶段 2.5（分布式基础设施）──→ 阶段 3（实例分域）──→ 阶段 4（Worker化）──→ 阶段 4.5（物理层）
                                                                                                              │
                                                                                      阶段 6（观测运维） ←──────┘
                                                                                              │
                                                                                      阶段 7（容量规划压测）
```

- **阶段 1 是最高优先级**：直接解决"几百玩家整档刷盘堵几分钟"
- **阶段 1.5 紧随其后**：资产安全是商业级底线
- **阶段 2/2.5** 可与阶段 1.5 并行设计，串行实施
- **阶段 2.5 是分布式部署的前提**：没有 lease/fencing/路由/节点注册，就不能多节点
- **阶段 3** 依赖阶段 2 的 catalog 基础
- **阶段 4/4.5** 依赖阶段 1+3 的分域基础
- **阶段 5** 在阶段 1+1.5 完成后即可开始
- **阶段 6** 可渐进补齐，每个阶段完成时补对应指标
- **阶段 7** 贯穿始终，但正式压测在阶段 4 之后

---

## 影响范围

### 必须同步修改的模块

| 包 | 影响文件/目录 |
|---|---|
| `packages/server/src/persistence/` | 全部重构，新增大量分域 service |
| `packages/server/src/runtime/player/` | `player-runtime.service.ts` — dirty 追踪改造、hydrate 改造 |
| `packages/server/src/runtime/world/` | persistence-state 改造、instance lifecycle 改造 |
| `packages/server/src/runtime/instance/` | `map-instance.runtime.ts` — 域级 dirty 改造 |
| `packages/shared/` | 可能需要新增持久化相关类型定义 |
| `packages/server/src/tools/` | 迁移脚本、smoke 测试更新 |

### 分布式部署新增模块

| 包 | 影响 |
|---|---|
| `packages/server/src/persistence/` | 新增 `DatabasePoolProvider`、`NodeRegistryService`、`PlayerSessionRouteService` |
| `packages/server/src/runtime/world/` | 新增跨节点转移协议、lease 管理 |
| `packages/server/src/network/` | Gateway 层玩家路由（如部署多节点） |
| 运维基础设施 | PgBouncer 配置、Redis Sentinel/Cluster 配置 |

### 不受影响的模块

- `packages/client/` — 纯前端，不涉及持久化
- `packages/config-editor/` — 配置编辑器
- 静态内容文件（`data/maps/`、`data/content/`）

---

## 终局规模对照检查

| 终局要求 | 覆盖阶段 | 状态 |
|---|---|---|
| 1000+ 玩家并发刷盘 | 阶段 1 分域 + 阶段 4 多 Worker | ✅ |
| 1000+ 地图实例 | 阶段 2 catalog + 阶段 3 分域 + 惰性恢复 | ✅ |
| 强资产零丢单 | 阶段 1.5 durable command chain | ✅ |
| 分布式多节点部署 | 阶段 2.5 节点注册/lease/路由/跨节点转移 | ✅ |
| 节点故障自动接管 | 阶段 2.5 lease 过期 + fencing | ✅ |
| Split-brain 防护 | 阶段 2.5 ownership_epoch + fencing guard | ✅ |
| 登录风暴处理 | 阶段 4 恢复队列 + 限流 | ✅ |
| 数据库连接池可控 | 阶段 2.5 统一 Pool + PgBouncer | ✅ |
| Redis HA | 阶段 4 Sentinel/Cluster | ✅ |
| 可观测可运维 | 阶段 6 指标 + GM 工具 | ✅ |
| 压测验证 | 阶段 7 容量规划 + 压测计划 | ✅ |
