# 旧实现残留清理计划

## 完成总结

基于 2025-05-12 全面排查，已完成阶段一～五和阶段六大部分清理：

| 阶段 | 内容 | 删除行数 |
|------|------|----------|
| 一 | 零风险删除（空文件、零引用代码、临时文件） | ~50 |
| 二 | 文档与配置修正（README、archive、SKILL.md） | — |
| 三 | 重复代码消除（targeting fallback、enhancement history、config-editor 常量） | ~20 |
| 四 | 废弃服务清理（MapPersistenceService、WorldPlayerSourceService、types.ts、valueStats） | ~745 |
| 五 | 旧通知链路迁移（~79 处纯文本→结构化 + 70 个语言包模板） | +170/-66 |
| 六-A | 灵气等级输入规范化（重命名，非删除） | — |
| 六-B | 离线收益旧字段删除（count/amount/expGain） | ~17 |
| 六-D | 战斗目标旧布尔字段删除 | ~168 |
| 六-E | 功法 attrCurves 旧格式删除 | ~246 |
| 七(部分) | 5 个未注册脚本删除 | ~770 |

---

## 剩余项

### 6-C：通知 text fallback 删除 🔒

**阻塞**：旧战斗服务（@ts-nocheck，战斗重构计划阶段 14）中约 14 个 notice 不带 `combat` 字段，客户端旧正则 fallback 是唯一渲染路径。

**解除条件**：战斗重构计划阶段 14 完成后，旧战斗服务和旧正则 fallback 一起删除。

### 6-F：TileType 旧地图格式删除 🔒

**阻塞**：TileType 有 798 处引用，是整个地图系统核心类型。

**解除条件**：[地图地块多层拆分改造计划](地图地块多层拆分改造计划.md) 完成后自然解决。

### 6-G：Protobuf 空壳决策

等团队决定是否正式启用 Protobuf 编码。启用则填充实现，不启用则删除空壳。

### 阶段七剩余

| 项 | 状态 | 解除条件 |
|----|------|----------|
| `import-legacy-persistence-once.ts` | 🔒 | 被 deploy-database-preflight 引用 |
| `convert-main-dump-to-current-db.ts` | 🔒 | 被 tools 引用 |
| `legacy_sync`/`legacy_backfill` 分支 | 🔒 | 被 auth service + smoke 大量使用 |
| 旧战斗服务瘦身/删除 | ⏳ | 战斗重构计划阶段 14 |
| GM 面板 legacy 投影适配 | ⏳ | 低优先级 |
| `config-editor/local-api.cjs` → TS | ⏳ | 低优先级 |
| `tools/` 180 个 @ts-nocheck 迁移 | ⏳ | 长期技术债 |

---

## 下一步

推进 **[地图地块多层拆分改造计划](地图地块多层拆分改造计划.md)** — 完成后解除 6-F，同时解决不动项中的 `LEGACY_MAP_TERRAIN_PROFILE_IDS`。
