# 关闭链路编排

状态：待补。

本文档后续记录 GM restart / SIGTERM / SIGINT 到进程退出的统一关闭编排。当前代码已有 `WorldShutdownDrainService` 执行 drain，但还没有被完整收进生命周期编排器。

预期覆盖：

1. 关闭 traffic 和新 bootstrap。
2. socket drain 与玩家 detach。
3. presence / session route / detached cache 收口。
4. 停止 tick。
5. 停止 worker / outbox / flush consumer。
6. final player flush / map flush / domain flush。
7. release instance leases。
8. node deregister。
9. 超时、失败、强制退出策略。

补齐前，不把本链路当成已完成编排合同。
