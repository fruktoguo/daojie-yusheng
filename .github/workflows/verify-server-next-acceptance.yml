name: Verify Server Next Acceptance

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify-replace-ready-acceptance:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    environment: server-next-shadow

    env:
      SERVER_NEXT_SHADOW_URL: http://${{ secrets.DEPLOY_SSH_HOST }}:11923
      SERVER_NEXT_GM_PASSWORD: ${{ secrets.PROD_GM_PASSWORD }}

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: 20

      - name: Enable Corepack
        run: corepack enable

      - name: Install dependencies
        run: pnpm install --frozen-lockfile || pnpm install

      - name: Doctor
        run: pnpm verify:replace-ready:doctor

      - name: Verify replace-ready acceptance
        run: pnpm verify:replace-ready:acceptance
