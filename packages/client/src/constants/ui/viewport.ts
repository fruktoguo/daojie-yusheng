/**
 * 本文件定义客户端常量或展示配置，是 UI、地图、输入和本地渲染共同依赖的稳定来源。
 *
 * 维护时要保持常量含义清晰，并同步检查消费方，避免把服务端权威规则复制成客户端私有真源。
 */
/**
 * 客户端桌面设计视口。
 * 桌面端 UI 与地图逻辑默认锚定在该分辨率下，再整体做等比缩放。
 */
export const DESIGN_VIEWPORT = {
  width: 1920,
  height: 1080,
} as const;
