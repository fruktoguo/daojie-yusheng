{
  "name": "@mud/client-next",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "generate:editor-catalog": "node ../../scripts/generate-editor-catalog.mjs --client=client-next --shared=shared-next",
    "generate:item-sources": "node ./scripts/generate-item-sources.mjs",
    "prebuild": "pnpm --filter @mud/shared-next build && pnpm run generate:editor-catalog && pnpm run generate:item-sources",
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "socket.io-client": "^4.7.0",
    "@mud/shared-next": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  }
}
