{
  "name": "@mud/config-editor",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "prebuild": "pnpm --filter @mud/shared build",
    "dev": "../client/node_modules/.bin/vite --host 127.0.0.1 --port 5174",
    "build": "../client/node_modules/.bin/tsc -p tsconfig.json && ../client/node_modules/.bin/vite build",
    "start:api": "pnpm --filter @mud/shared build && node --watch local-api.cjs"
  },
  "dependencies": {
    "@mud/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  }
}
