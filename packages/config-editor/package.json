{
  "name": "@mud/config-editor",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "prebuild": "pnpm --filter @mud/shared-next build",
    "dev": "vite --host 127.0.0.1 --port 5174",
    "build": "tsc -p tsconfig.json && vite build",
    "start:api": "pnpm --filter @mud/shared-next build && node --watch local-api.cjs"
  },
  "dependencies": {
    "@mud/shared-next": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  }
}
